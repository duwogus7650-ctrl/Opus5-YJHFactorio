# 빌드 산출물은 커밋한다 (dist/Logic-Foundry.html 이 곧 게임이다).
# 그 외 기계가 만든 것만 제외한다.
__pycache__/
*.pyc
.venv/
node_modules/

# 에이전트 세션 상태 — 이 레포와 무관하고 세션마다 바뀐다
.omc/

# 스크린샷은 커밋한다 (README 가 참조하고, 눈으로 검수한 증거다)
# shots/ 는 제외하지 않는다
tests/.mutate-backup/
