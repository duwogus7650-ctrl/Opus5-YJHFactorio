# 작업 트리를 어느 PC에서든 LF 로 고정한다.
# 이유: tests/mutate.py 의 돌연변이 앵커가 여러 줄짜리 LF 문자열이라,
# CRLF 로 체크아웃되면 앵커가 하나도 안 맞고 그 돌연변이가 INVALID(검정 불성립)로
# 조용히 빠진다 — 검정한 척만 하게 된다. 실제로 다른 PC 클론에서 6건이 그렇게 빠졌다.
* text=auto eol=lf
*.png binary
