# LOGIC FOUNDRY — 진행 상태

## 완료 (2026-08-07 ~ 09)

- 코어·세계생성·벨트·기계·전력·제어기·전투·렌더·UI·저장 전부 구현, 스텁 없음
- **출고 전 적대적 리뷰**(5차원 병렬 + 반증 검증)에서 critical 3 · high 5 확정, 전부 수정
- 밸런스 결함 3건 수정: 나무 오염흡수 60배 과다(습격이 사실상 안 왔다) · 진화도 선형 clamp →
  점근형 · 파도 크기 상한. 초반 5분 유예 추가
- 저장/불러오기 **UI 진입점** 추가 (F2/F3 + 창고 패널 버튼) — 있었지만 부를 방법이 없었다
- **문장(규칙) 편집기** — 코딩을 안 해 본 사람이 빈칸만 고르면 회로가 생긴다. 컴파일러가
  연구 안 된 노드를 거부하고, 규칙끼리 노드가 겹치지 않게 실제 사용 높이만큼 띄운다
- **자력 완주 주행 결정론화** — 드라이버가 프레임 루프를 세우고 시뮬을 직접 2초씩 민다.
  같은 코드 두 번 돌려 녹팩 78~260 으로 흔들리던 것이 연속 2회 전 항목 일치로 고정
- **교차 브라우저**: Edge·Chromium·Firefox·WebKit 4엔진 × 드라이버 2개 = 8조합 전부 GREEN
- 납품: `Desktop\DCT\[DCT] Logic Foundry.html` · GitHub 비공개 레포

## 이번에 추가한 것 (2026-08-09, 제어기 노드 확장)

노드 25종 → **29종**. 셋 다 제어기의 표현력을 넓히는 축이고, 새 건물·새 자원은 없다.

- **평활 필터** (`smooth`, 논리 III) — 1차 저역통과. 시상수 τ, 계단응답 `1-e^(-t/τ)`.
  오일러 근사가 아니라 지수해라서 **dt 를 어떻게 쪼개도 같은 값**이고, 그 성질이 곧
  "dt 를 곱했는가" 게이트가 된다
- **상태기계** (`fsm`, 논리 II) — 4단계 SFC. 전이는 상승엣지(레벨이면 60 Hz 로 돈다),
  리셋은 레벨이고 전이보다 우선, 단계 출구는 원핫
- **신호 버스** (`bussend`/`busrecv`, 논리 III) — 제어기끼리 8채널(A~H).
  **합산 + 1틱 지연**이라 제어기 평가 순서를 타지 않는다. 채널은 고정 목록(오타 침묵 방지)
- 새 기술은 만들지 않았다 — `clear.js` 가 "연구 8종 = 410 사이클"을 오라클로 쓰고 있어
  9번째 기술은 자력완주 게이트의 기준식을 통째로 흔든다
- 자력 완주 주행에 제어기 4를 추가했다. 노드 전 종류를 살아 있는 회로에 걸어야 통과하는
  게이트(`clear.allNodeKindsWired`)가 있어서, 종류를 늘리면 주행도 같이 늘어야 한다

### 검증에서 실제로 갈린 자리

노드를 늘리자 **덫 두 개가 순서대로 울렸다.** 둘 다 "안 써 본 기능은 안 깨진 기능이 아니라
아직 안 들킨 기능"이라는 이 레포의 규율이 코드로 박혀 있던 것이다.

- `sweep.allNodeKindsKnown` (`fullplay.js`) 이 네 엔진에서 RED — 노드 종류마다 단독 시험을
  두는 파일인데 새 4종의 시험이 비어 있었다. 모델 게이트 162건이 전부 GREEN 인 상태에서
  걸렸다. 채워 넣어 33건이 됐다.
- `clear.allNodeKindsWired` 가 25/25 → 29/29 로 기준이 올라갔다. 제어기 4를 세워 통과.

그 다음 **적대적 리뷰**(5렌즈 + 발견마다 독립 반증)에서 43건 중 10건이 확정됐고, 그중 둘은
게이트가 GREEN 인 채로 살아 있던 진짜 결함이었다 — 평활 필터가 배선 전에 y=0 으로 굳는 것과,
저장 게이트가 두 결함의 상쇄로 통과하던 것. 자세한 것은 `tasks/lessons/15`.

**이번 작업에서 이 PC 환경 때문에 새로 드러난 것**: 클론이 CRLF 로 체크아웃돼 돌연변이
앵커(LF 기준, 여러 줄)가 6건이나 안 맞았고, 그 6건은 FAIL 도 MISS 도 아닌 INVALID 로 빠져
있었다 — 검정한 척만 하는 상태다. `.gitattributes` 로 작업 트리를 LF 에 고정하고,
`build.py` 가 dist 를 쓸 때 `newline='\n'` 을 명시해 배포본 바이트가 플랫폼·설정에
휘둘리지 않게 막았다. 같은 종류로, 완주 주행이 300초 브라우저 상한 **바로 아래**에서 끝나고
있었다(실제로 한 번 TIMEOUT 이 났다). 타임아웃은 판정 불성립이라 `LF_TIMEOUT` 으로 열었다.

### 최종 검증 (2026-08-10, 배포본 바이트 기준)

```
모델 197 · 클릭 76 · 전수 35 · 부하차단 10       GREEN (각 고의 실패 1건 정상 검출)
돌연변이 121건                                   GREEN (잡음 121 · 놓침 0 · 무효 0)
교차 브라우저 18조합                             GREEN (4엔진 × 4드라이버 + 터치 2)
완주 주행 clear.js                               RED 3건 — 직전 대조군과 동일(드라이버 물류 모델)
```

## 남은 것

- [ ] **사람이 직접 30분 플레이해 재미를 확인.** 이건 기계로 못 잰다. 페이싱 표(README)는
      "규칙이 맞는 속도인가"만 보증한다. 특히 볼 것: 제어기 첫 배선까지 얼마나 걸리는가,
      6분 첫 습격이 실제로 긴장되는가, 노드 편집기가 손에 붙는가.
- [ ] 실기 Safari(맥·아이패드)와 모바일/터치. WebKit 엔진과 터치 게이트는 통과했지만
      실기와는 별개다. WebKit·Firefox 의 터치 검사는 여전히 '검증 불가'로 보고된다.
- [x] 새 노드를 **문장 편집기**에도 넣었다 — 평활 필터(계산 한 단)와 신호 버스(읽기+행동).
      상태기계는 뺐다: 문장 한 줄은 조건 하나에 행동 하나인데 전이가 넷이라 한 줄로 접으면
      문장이 아니라 표가 된다. 도움말에 "회로 화면에서 쓴다"고 적었다.
      그 과정에서 **표에만 있고 못 쓰던 기능 둘**이 나왔다 — '숫자를 띄운다' 가 참/거짓을
      띄우고 있었고, 계산 한 단은 화면에 고를 자리가 아예 없었다.

## 이번에 추가한 것 (2026-08-09, 유체 — 증기 발전)

건물 13종 → **17종**(파이프·지하수 펌프·보일러·증기기관), 노드 30종째로 **유체 잔량** 센서.

- 유체망은 전력망의 유니온-파인드를 그대로 이식했다. **맞닿음이 곧 연결**이고 망 하나는
  잘 섞인 탱크 하나다 — 압력 기울기도 흐름 방향도 없다(전력망이 선로 손실을 안 보는 것과
  같은 층위). 유체는 회원 건물이 제 몫을 들고 있어 저장이 그냥 된다
- Factorio 값 넷이 **하나의 항등식**으로 묶인다: `1800/60 = 30 = 900/30` → 증기 1개 = 30 kJ.
  게이트가 양쪽에서 따로 재서 대조한다(실측 30.00)
- **버퍼가 선행 지표를 만든다** — 수요 858 kW 에서 증기 50% 일 때 전력은 아직 100%,
  전력이 처음 무너질 때 증기는 이미 0%. 그래서 "모자란 뒤에 끄기" → "마르기 전에 끄기"
- 새 기술은 안 만들었다(clear.js 의 410 사이클 오라클). 전부 `steel` 에 편입
- **물 지형은 되돌렸다** — 리그 고정 좌표(스폰에서 최대 85타일)와 양립 불가. 원장 참조

## 이번에 추가한 것 (2026-08-10, 청사진)

`B` / [청사진] 버튼 → 영역 드래그 → 좌클릭 붙여넣기. 저장에도 실린다.

- **값은 배선이다.** 제어기의 규칙·그래프가 따라오고, 영역 **안** 참조는 사본으로 갈아
  끼우고 영역 **밖** 참조는 끊는다(안 끊으면 사본이 원본 기계를 지배한다)
- 재료는 실제로 나간다 · 내용물은 안 따라온다(복제기 금지) · 막힌 칸은 그것만 건너뛰고 이유 반환
- **회전은 없다** — 분배기 2칸 셀·벨트 방향 재계산이 별개의 위험 구간이라 뺐다

## 확장 후보 (우선순위 순)

1. ~~**기차**~~ — 2026-08-10 완료(위 절).
2. **청사진 회전** — 담은 것을 90도씩 돌려 붙이기. 분배기 셀과 벨트 방향을 같이 돌려야 한다.
3. 유체 확장: 저장 탱크(버퍼를 플레이어가 키울 수 있게), 펌프로 망 사이 이송, 석유·화학.
   탱크는 "버퍼를 얼마나 둘 것인가"를 플레이어의 설계 결정으로 만든다 — 제어 문제가 한 번 더 깊어진다.
4. 제어기 노드 더: 신호 버스를 **채널별 이름표**로 쓰기, 다단 상태기계, 이동평균(박스카).
   박스카는 저장 크기를 먼저 봐야 한다 — 저장이 이미 localStorage 한 칸(약 140 KB)에 근접.

## 손대면 안 되는 것

- `src/05_data.js` 의 수치표는 Factorio 공개값이 오라클이다. 바꾸면 처리량 게이트가 어긋난다.
  밸런스를 바꾸고 싶으면 게이트의 기대식도 같이 옮기고, 왜 옮겼는지 적는다.
- 기술 트리에 새 항목을 추가하는 것도 같은 종류다 — `clear.js` 의 연구 사이클 계산이 오라클로
  걸려 있다. 노드를 늘릴 때는 기존 기술에 편입시키는 편이 싸다.
- 틱 순서(로직 → **유체** → 전력 → 기계 → 벨트 → 오염 → 적). 로직이 뒤로 가면 부하 차단이
  한 틱 늦어 전력이 진동하고, 유체가 전력 뒤로 가면 증기기관의 공급이 한 틱씩 밀린다.
- 신호 버스의 **1틱 지연과 합산**. 둘 중 하나라도 풀면 제어기 평가 순서가 값을 정하게 되는데,
  그 순서는 화면 어디에도 안 나온다.

## 이번에 추가한 것 (2026-08-10, 기차)

건물 17종 → **20종**(레일·역·열차), 노드 32종째로 **역 상태** 센서와 **열차 출발** 행동.
원거리 광맥을 벨트로 잇는 대신 실어 나르게 됐다.

- **한 노선에 한 대**만 상정한다. 신호기·다중 편성·교차 통제·대각선 곡선이 전부 없다 —
  넣으려면 열차끼리의 상호배제가 필요하고, 그건 이 게임이 가르치려는 축(제어기)이 아니라
  별개의 규칙 덩어리다. 못 하는 것을 README 에 그대로 적었다
- **열차는 점유맵 밖에 산다.** 한 칸에는 엔티티 id 하나뿐인데 그 자리를 레일이 쓰고 있어서,
  열차를 점유맵에 넣으면 지나갈 때마다 레일 점유가 지워진다. `trainAt()` 로 따로 찾고
  인서터 네 갈래가 전부 그것을 보게 했다
- 기본 출발 규칙은 "가득 찼거나 5초"인데, 역에 **[열차 출발]** 노드를 걸면 제어기가 그
  판단을 가져간다. 지배는 매 틱 풀리므로 노드를 지우면 곧바로 기본 규칙으로 돌아온다

### 검증에서 실제로 갈린 자리 — 돌연변이가 게이트 셋을 반증했다

코드가 아니라 **내 게이트가 약했다.** 셋 다 `tasks/lessons/16` 에 적었다.

- **기대값을 게임에서 읽고 있었다.** `Math.abs(travelSec - 20 / SP.trainSpeed)` 는 속도를
  8→12 로 바꿔도 양변이 같이 움직여 차이가 언제나 0 이다. 대조가 아니라 항등식이었다.
  설계값 8·5 를 시험 파일에 상수로 박아 고쳤다
- **관측 창이 0.125초뿐인 자리에서 재고 있었다.** 노선 한가운데로 인서터를 옮기고
  60초(여러 왕복)를 돌려 창을 구조적으로 넓혔다
- **이중 방어가 서로를 가렸다.** 레일 검사가 UI 와 모델 양쪽에 있어 한쪽만 깨면 다른 쪽이
  막는다. UI 를 우회하는 모델 게이트(`train.addRejectsNonRail`)를 세워 층마다 따로 쟀다

### 최종 검증 (2026-08-10, 배포본 바이트 기준)

```
모델 209 · 클릭 78 · 전수 37 · 부하차단 10       GREEN (각 고의 실패 1건 정상 검출)
돌연변이 130건                                   GREEN (잡음 130 · 놓침 0 · 무효 0)
교차 브라우저 18조합                             GREEN (4엔진 × 4드라이버 + 터치 2)
완주 주행 clear.js                               RED — 아래 "완주 드라이버 커버리지" 절 참조
```

## 이번에 메운 것 (2026-08-10, 완주 드라이버 커버리지)

기차를 커밋한 직후 40분 완주 주행이 RED 4건이었고, **둘은 커버리지 구멍**이었다.
유체(건물 4종)와 기차(건물 3종·노드 3종)를 넣으면서 `clear.js` 를 안 늘려, 기준만
건물 20종·노드 32종으로 저절로 올라가고 주행은 13종·29종에 머물렀다.

- 증기 발전소 단계(펌프·보일러·증기기관·파이프)와 철도 단계(레일 6칸·역 2대·열차),
  그리고 그것들을 보는 제어기 5(유체 잔량·역 상태·열차 출발)를 드라이버에 넣었다
- **게이트를 둘 더 세웠다** — `steamPlantRan`(증기 최대 잔량 > 0)과
  `trainRanRoute`(열차 이동 ≥ 10타일). 커버리지 게이트는 "지었다"만 세므로 연료 없는
  보일러와 붙박이 열차를 통과시킨다. 새 게이트는 처음 실행에서 둘 다 RED 였다
- 결과: **건물 20/20 · 노드 32/32 · 증기 1199.8 · 열차 65~97타일** (6회 주행 내내 안정)

### 여기서 실제로 갈린 자리 — 넷 다 드라이버의 경제 문제였다

- **흐름이 넉넉한 것과 재고가 있는 것은 다르다.** 40분 내내 `철판 0/40`. craft 가 쓰고
  feed 가 기계 버퍼로 밀어 넣어 재고가 한 번도 안 쌓인다. 구매를 사이클 **앞**으로
  옮기고, 기계가 안 받는 품목(파이프)으로 저축하고, 부품별로 사되 **자리를 못 박았다**
- 자리를 바꾸며 부분 건설했더니 펌프·보일러를 40분 내내 반복 건설해 철판이 0 이 되고
  탄약이 끊겼다. 누수의 원인은 부분 건설이 아니라 자리를 바꾼 것이었다
- **`CAP.steel = 8`** — 분배기(강철 2)만 있던 시절의 상한이 강철 재고 9 에서 제련로를
  영원히 건너뛰게 했다. 목표량과 상한을 같은 식에 묶었다(`STEEL_WANT + 4`)
- 보일러는 발전기와 석탄을 나눠 먹는다(1.8MW 태워 900kW). 발전기 먼저, 재고 40 초과일
  때만 보일러에 조금씩

### 그러다 드러난 것 — 비재현성, 그리고 그 원인 (2026-08-10 해결)

코드를 안 바꾸고 6회 돌렸는데 6회 다 달랐다(습격 손실 3~10, 최저 전력 46~94%).
`powerHeld` 가 주행마다 PASS/FAIL 을 오갔다. 직전 커밋의 드라이버도 같았다 — README 가
버전 차이로 적어 두었던 47.9% 와 90.7% 는 같은 코드의 두 주행이었다.

**원인: `newGame()` 이 오염 확산 타이머 `pollTimer` 를 안 되돌렸다.** 페이지가 열리고
드라이버가 리셋하기까지 실시간으로 돈 시간이 [0,0.25) 의 잔여 위상으로 남아, 확산·감쇠
스텝이 매 주행 다른 게임 시각에 떨어졌다. 오염 0.1% 차이가 진화도 → 습격으로 증폭된다.
찾기 어려웠던 이유는 `G.stateHash()` 가 오염 격자를 안 담아서 지문이 그 차이를 못 봤기
때문이다(지금은 격자 총량과 진화도를 담는다).

- 고친 뒤 40분 주행 2회가 게이트 13건·지문 40개 전부 일치
- 새 게이트 `tests/determinism.js` 7건 — 핵심은 `det.resetClearsPriorRun`
  (리셋 전에 7.3초를 돌려도 같은 결과). 되돌린 빌드에서 FAIL 확인, 돌연변이 131번 등록
- 음성 대조군이 이 게이트의 첫 판을 잡았다 — 리그가 빈약해 씨앗을 바꿔도 지문이 같았다

자세한 것은 `tasks/lessons/17` 과 원장.

## 이번에 고친 것 (2026-08-10, 방어 되먹임)

완주 주행이 재현되기 시작하자 남은 실패를 실제로 추적할 수 있게 됐다. 셋 중 둘이
같은 뿌리였다 — **터렛이 작업 줄에 매달려 피해가 난 뒤에 섰다.**

```
t=1063  터렛 4기 · 잃은 건물 1
t=1129  터렛 4기 · 잃은 건물 6 · 발전기 5→2대 · 전력 만족도 48%
t=1197  터렛 8기 (이미 늦었다)
```

전력 게이트 실패의 원인은 석탄도 발전기 수도 아니라 발전기가 부서진 것이었다.

- `autoGen` 옆에 `autoTurret` — 진화도에 비례해(기본 6기 + 진화 6%당 1기) 미리 세운다.
  이 파일이 스스로 "방어는 계획이 아니라 되먹임이어야 한다"고 적어 놓고 줄에 두고 있었다
- 터렛(철판 20)도 **재고**가 있어야 산다 — 목표 10기인데 5기에서 멈춰 있었다.
  증기 발전소 때와 같은 문제라 같은 해법(`ironHold` 32)을 썼다
- 제어기 몫(철판 12)을 안 남기면 제어기 2·4 가 못 서서 노드 커버리지가 32 → 29 로
  후퇴한다. **방어를 고치며 커버리지를 깨면 고친 게 아니다**
- 게이트가 손실 개수만 말하던 것을 **뚫린 자리**까지 말하게 했다(`lostList`).
  게임은 그 목록을 이미 들고 있었는데 드라이버가 안 봤다

결과: 13건 중 11건 통과 (전 3건 실패 → 2건). 손실 10 → 1, 최저 전력 46.1% → 96.7%,
연구 6 → 7종. 두 주행이 완전히 동일하다.

남은 2건: 연구 7/8(드라이버 물류 모델의 한계) · 손실 1(`turret@101,65` — 공장이 아니라
바깥 고리에 혼자 선 터렛). 고리 기하는 두 번 손댔다가 둘 다 더 나빠져 되돌렸다.

## 이번에 추가한 것 (2026-08-10, 청사진 회전)

`R`(폰은 [회전] 버튼)로 90도씩. 건물·노드는 안 늘었으니 커버리지 기준은 그대로다.

- **좌표와 방향을 함께 돌린다.** 하나만 돌리면 라인 모양은 맞는데 흐름이 옛 방향이거나,
  제자리에서 엉뚱한 곳을 가리킨다
- 크기는 정의값이 아니라 **지금 방향 기준의 실제 발자국**을 쓴다(분배기는 dir 1·3 에서
  1x2 로 선다). 그 규칙을 다시 쓰지 않고 `canPlace` 와 같은 식을 부른다
- `R` 은 원래 도구 방향 키다. 붙여넣기 중에는 청사진을 돌리고, **키와 버튼이 같은
  손잡이(`rotateAction`)를 쓴다** — 키에만 갈림길을 넣었더니 폰에서 회전이 안 닿았다

### 게이트를 세우며 두 번 헛디뎠다 (둘 다 돌연변이가 짚었다)

- 담는 영역을 **4x4** 로 잡아 `rotateSwapsFootprint` 가 "4x4 → 4x4" 로 통과했다.
  정사각형에서는 가로세로가 바뀌어도 같은 수다 → 4x3 으로 바꿨다
- `rotateKeepsCost` 가 **자기 자신과 비교**하고 있었다 → 회전 전 값을 미리 찍어 뒀다
- 리그에 **방향 1 짜리 분배기가 없어서** 발자국 규칙을 지우는 돌연변이가 살아남았다
  (MISS) → 규칙이 적용되는 입력을 리그에 넣었다
- "4회전 항등이 방향 회전도 잡는다"고 주석에 적었는데 **틀렸다** — 방향을 아예 안 돌리면
  네 번 뒤에도 원본과 같다. 돌연변이가 그 주장을 반증해서 주석과 기대 게이트를 고쳤다

검증: 모델 215 · 클릭 79 · 전수 37 · 부하차단 10 · 재현성 7 · 터치 26 GREEN,
회전 관련 돌연변이 6건 전부 CAUGHT.

## 이번에 추가한 것 (2026-08-10, 저장 탱크)

건물 20종 → **21종**. 파이프 한 칸이 100 인데 탱크는 **25,000**(Factorio 공개값) —
파이프 250칸어치라 증기 60/s 기준 7분 가까운 완충이다. **얼마나 둘 것인가가 설계 결정**이
되고, 그만큼 제어 문제가 깊어진다.

- 오라클 둘: 용량은 설계값 25,000 을 시험 파일에 숫자로 박아 대조하고, **완충은 시간**이라
  연료를 끊은 뒤 버티는 시간이 `저장량 ÷ 소비(30/s)` 여야 한다(실측 837 → 27초 / 예상 28초)
- 게이트를 세우며 세 번 헛디뎠다: 전기 수요가 없으면 증기기관은 증기를 안 쓴다(400초
  그대로 버팀) · 조립기 출력 버퍼가 차면 수요가 사라진다(차면 멈추는 부하는 부하가 아니다) ·
  3x3 탱크를 보일러와 겹치게 둬 배치가 조용히 실패했다(배치 실패와 용량 오류가 같은 얼굴)

### 완주 드라이버는 탱크를 못 산다 — 9회 조정 끝에 RED 로 남겼다

교훈 17 의 규율대로 완주 드라이버에 탱크 단계를 넣었지만, 이 경제는 탱크(철판 20 +
강철 5 = 철 45장어치)를 감당하지 못한다.

```
탱크: 재료 대기 — 강철 24/5 철판 0/20
```

강철은 남는데 **철판 재고가 한 순간도 20 에 못 닿는다**. 강철 예산을 26·28·30 으로
올려도, 노선을 6→4칸으로 줄여도, 탱크용 철판 재고를 잡아도(ironHold 는 craft 만 막고
runJobs 의 건물 비용은 못 막는다) 안 됐다. 강철 1개는 철판 5장이고 그 철판은 회로를
거쳐 제어기가 되므로, 예산을 올릴 때마다 제어기 2·4 가 못 서서 노드 커버리지가
32 → 29 로 떨어졌다. **강철 예산은 곧 제어기 예산이다.**

게이트를 약화시켜 통과시키는 대신 RED 를 남기고, 게이트가 이유를 말하게 했다.
진짜 해법은 드라이버의 물류 모델을 고치는 것이고(`tasks/lessons/12`) 그건 별도 작업이다.

검증: 모델 218 · 클릭 79 · 전수 37 · 부하차단 10 · 재현성 7 GREEN,
탱크 돌연변이 2건 CAUGHT.

## 이번에 고친 것 (2026-08-10, 제철 되먹임)

**병목은 물류가 아니라 철이었다.** 생산 내역을 떠 보니 철판 5847장 중 톱니 2612(45%)·
탄약 1256(21%)이 나갔고, 녹색 연구팩의 병목은 벨트 128개였다 — 벨트는 톱니를 거쳐
철에서 온다.

- `autoGen`·`autoCoal` 옆에 **`autoIron`** — 초반(t<1200)에 최대 6쌍(채광기+용광로).
  한 쌍은 철판 15장을 쓰고 20분에 370장을 돌려준다
- 결과: **저장 탱크를 살 수 있게 돼 건물 21/21 · 노드 32/32** (게이트 10 → 11건 통과)
- 대가: 기계가 늘어 오염↑ → 첫 습격이 세짐 → 손실 1 → 3
- **"터렛 4기 뒤에 증설" 은 실패했다** — 회수 20분이 모자라 연구 7→6종, 탱크 못 삼,
  노드 32→29(9건 통과). 철은 이르게 늘려야 값이 나온다

남은 2건: 연구 7/8 · 손실 3. 손실 중 둘은 **첫 습격 때 터렛이 1~2기뿐**이라 뚫린 것이고,
습격 유예 300초와 군수 연구 완료 t≈400 사이의 구조적 구멍이다 — 연구 순서나 유예를
손대야 하는 별개 문제.

## 이번에 추가한 것 (2026-08-10, 변화율 노드)

노드 32종 → **33종**. **얼마나 남았나가 아니라 얼마나 빨리 줄고 있나**를 잰다.
완충(탱크)이 커질수록 수위는 느리게 움직여 신호로 약해지는데, 그때 쓰는 것이 기울기다 —
"증기가 초당 12씩 줄고 3,000 남았다 → 250초 뒤 마름"의 분모를 만든다.

- 문장 편집기에도 넣었다(`초 창으로 잰 초당 변화`). 단항이 둘이 되면서 컴파일러가
  `smooth` 를 손으로 박아 두던 자리가 갈림길이 됐다 — 정의에 노드 이름·설정 열쇠를
  적어 컴파일러가 읽게 했고, 돌연변이로 그 갈림길을 검정했다
- 완주 드라이버(제어기 3)에 배선해 노드 33/33 유지

### 오라클을 두 번 갈아 끼웠다

타이머의 `위상%` 로 기울기를 재려 했는데 그 출력은 **정수로 반올림된 계단**이라
(`Math.round`) 대부분의 틱에서 변화가 0 이고 가끔 1 씩 뛴다. 날것의 변화율이 0.000 으로
나왔고 평활을 걸자 주기 경계의 100→0 이 튀어 음수가 됐다 — **계단을 미분해 놓고 미분이
틀렸다고 읽을 뻔했다.** 취수 펌프의 물(1200/s, 연속량)로 바꾸자 1200.0 이 나왔다.

검증: 모델 224 · 클릭 79 · 전수 38 · 부하차단 10 · 재현성 7 GREEN,
변화율 돌연변이 4건 CAUGHT. 완주 주행 11/13 유지.

## 이번에 고친 것 (2026-08-10, 첫 습격 손실 0)

직전까지 "습격 유예 300초와 군수 연구 완료 t≈400 사이의 **구조적 구멍**"이라고 적었던
것이 틀렸다. 군수는 적팩 20개면 되고 적팩은 구리 1 + 톱니 1 인데, **시작 재고에 구리 30 ·
톱니 30 이 이미 있다** — t≈150 에도 낼 수 있다. 늦은 것은 드라이버였고 이유는 둘이다.

- 적팩 제작이 `cuLine`(구리 용광로가 구웠는가)을 기다렸다. 그 가드의 원래 목적(구리를 다
  써서 전주가 막히는 교착)은 **전주 몫 12 를 남기는 것**으로 지킬 수 있다 → 군수 t≈350
- **안쪽 터렛 고리를 위협 순이 아니라 동·남·서·북 고정 순서로 지었다.** 바깥은 둥지
  거리로 정렬하면서 안쪽만 고정이라 3기 시점에 북쪽이 비었고, 잃은 발전기(74,61)·
  전주(77,65)가 정확히 그 방향이다. 안쪽도 정렬하자 **손실 3 → 0**

결과: **13건 중 12건 통과** (두 주행 동일). 남은 1건은 연구 7/8.

> "구조적"이라고 부르기 전에 그 구조가 정말 막혀 있는지 계산해 본다 —
> 여기서는 시작 재고를 세어 보는 것만으로 아니었다.

## 남은 한 건 (2026-08-10, 연구 7/8)

연구 총수요 **적팩 410 · 녹팩 280** vs 생산 **적팩 345 · 녹팩 395** — 녹팩은 남고
적팩이 65개 모자란다. 새로 넣은 조립기 진단이 원인을 짚었다: `sci-red:.:in50`
(적팩 조립기 셋이 구리만 50 을 쥐고 톱니 0 으로 멈춤).

세 시도 모두 실패:
- 배분 정렬 뒤집기 → 공장 배분이 흔들려 배치 실패 2건 · 열차 정지, 연구는 그대로
- 톱니 재고 24 확보 → 톱니는 터렛 재료라 방어 붕괴(손실 0 → 24)
- 제철 증설은 이미 반영됨(그 덕에 녹팩은 남는다)

**적팩과 터렛이 같은 톱니를 다투고 터렛이 먼저다.** 남은 길: 톱니 생산 증설, 또는
탄약 소비 절감(탄약이 철판의 21%). 진단(`asmDiag`)을 상시로 남겨 뒀다.

### 연구 8/8 — 다섯 번째 실패까지 (2026-08-10)

앞의 셋(배분 정렬 뒤집기 · 톱니 재고 확보 · 제철 증설)에 둘을 더 시도했고 둘 다 실패:

- **톱니 조립기 증설(초반)** — 시작 재고로 t=0 에 3대를 지어 부트스트랩이 사라졌다
  (건물 4/21 · 연구 0/8). **되먹임 문턱은 시작 재고로 성립하면 안 된다**
- **톱니 조립기 증설(중반)** — 조립기 구역이 꽉 차 계획의 녹팩 조립기가 밀렸다.
  이 공장은 **공간도 한계**다
- **탄약 줄이기** — 손실 0 → 127, 전력 0%, 연구 7 → 4종. 격추 170·손실 0 이라는
  여유는 탄약을 채워 둔 **결과**였다. 결과 지표를 원인의 여유로 읽으면 안 된다

확정: 완주 주행 **12/13**, 남은 한 건은 적팩 65개 부족. 다음에 손댄다면 구역 확장
(EAST_RC/WEST_RC)이 먼저다 — 자재보다 자리가 먼저 막힌다.
