# Failure Ledger — 실패·막다른길 원장 (append-only)
새 항목은 파일 끝에 추가. 항목 삭제 금지. 틀린 내용은 해당 항목에
`정정 YYYY-MM-DD:` 줄을 덧붙여 바로잡는다.

## 2026-08-09 — 검증 결과를 기다리지 않고 커밋해, 구문이 깨진 테스트 드라이버가 커밋에 들어갔다
- 시도: 문장 편집기 작업을 마치고 회귀를 백그라운드로 돌린 뒤, 결과가 나오기 전에 커밋했다
- 실패 이유: 직전에 게이트를 다시 쓰면서 파이썬 문자열에 진짜 줄바꿈이 섞여 tests/driver.js 가 파싱 불가 상태였다. 하네스는 '게임이 부팅조차 못 했다'만 말하고 원인을 안 알려줘서, 게임 소스를 먼저 의심하느라 시간을 썼다
- 대안·다음엔: 커밋은 회귀 결과를 **읽은 뒤**에만 한다. 그리고 하네스가 결과를 못 찾을 때는 게임 소스뿐 아니라 **주입한 드라이버 자체의 구문**을 먼저 검사한다 — node -e 'new Function(fs.readFileSync(f))' 한 줄이면 즉시 갈린다
- 재사용 자산: 드라이버 구문 검사 한 줄 — node -e \"new Function(require('fs').readFileSync('tests/driver.js','utf8'))\"
- 참조: [[logic-foundry-game]]

## 2026-08-09 — 새 클릭 게이트를 파일 중간에 끼워 넣어, 뒤따르던 게이트를 무너뜨렸다
- 시도: 새 노드 4종의 팔레트·잠금 검사를 uismoke.js 편집기 절 한가운데에 삽입했다. 그 검사는 "연구 전 잠김"을 보려고 G.reset() 으로 판을 새로 깔았다
- 실패 이유: 리셋이 앞 절의 제어기를 지우고 nextEntId 까지 되돌려, 새 제어기가 **같은 id** 를 받았다. 그래서 뒤의 gAdd/gLink 가 엉뚱한 그래프에 노드를 심었고 ui.feedbackShownImmediately 가 RED 가 됐다 — 게이트가 겨냥한 코드는 멀쩡했다. id 재사용이라 호출이 전부 성공해서 증상이 '지웠는데 동작한다' 로 보였다
- 대안·다음엔: 판을 새로 까는 검사는 그 파일 **끝**으로 보낸다. 검사는 앞의 상태를 소비만 하고 뒤의 상태를 바꾸지 않는 자리에 둔다. 문장 편집기에서 같은 실패를 한 번 했으니 두 번째다
- 재사용 자산: tasks/lessons/14-a-check-that-resets-the-world-breaks-the-next-one.md
- 참조: [[logic-foundry-game]]

## 2026-08-09 — 새 게이트가 또 틀린 성질을 재고 있었다 (설정 라벨을 해석 줄로 착각)
- 시도: 신호 송신 노드에 '대상이 비어 있다' 오탐이 없는지 보려고 노드 전체 textContent 에서 '채널' 이라는 글자를 찾았다
- 실패 이유: 그 글자는 설정 드롭다운 라벨에도 있다. 해석 줄이 통째로 비어 있어도 통과하는 검사였다 — 실제로 처음 통과했을 때 해석 줄은 비어 있었다(updateLive 가 아직 안 돌아서)
- 대안·다음엔: 해석 줄은 [data-mean="<nid>"] 한 요소가 들고 updateLive() 가 채운다. 그 요소를 직접 읽고 채널명과 값이 함께 나오는지로 판정한다. 게이트마다 '이 성질을 만족하면서 틀릴 수 있는 구현이 있는가' 를 묻는다
- 재사용 자산: G.ui.updateLive() — 해석 줄을 강제로 한 번 갱신하는 테스트 훅
- 참조: tasks/lessons/13-a-gate-can-measure-the-wrong-property.md

## 2026-08-09 — 다른 PC 클론이 CRLF 라, 돌연변이 6건이 조용히 '검정 불성립'으로 빠졌다
- 시도: 이 레포를 새 PC(C:\Users\Admin)에 clone 한 뒤 tests/mutate.py 를 돌렸다
- 실패 이유: git 의 core.autocrlf=true 로 작업 트리가 CRLF 로 체크아웃됐는데, 돌연변이 앵커 중 **여러 줄짜리**는 LF(`\n`) 로 적혀 있어 하나도 안 맞았다. 그 6건은 FAIL 도 MISS 도 아닌 INVALID 로 빠지는데, 이건 "게이트가 실패를 잡는지 확인하지 않았다" 는 뜻이라 조용히 검정 구멍이 된다. 원인이 코드가 아니라 체크아웃 설정이라 소스를 아무리 봐도 안 보인다
- 대안·다음엔: (1) 작업 트리를 LF 로 정규화, (2) `.gitattributes` 에 `* text=auto eol=lf` 를 박아 어느 PC에서 clone 해도 LF 로 오게 함, (3) build.py 가 dist 를 쓸 때 `newline='\n'` 을 명시 — 기본값은 os.linesep 이라 배포본 바이트가 플랫폼에 따라 달라진다. 앵커를 LF 로 유지하는 대신 환경을 고정하는 쪽을 골랐다
- 재사용 자산: .gitattributes(사유 주석 포함) · 앵커 점검 한 줄 — mutate.py 를 import 해 MUTATIONS 를 돌며 `src` 파일에서 find 문자열이 정확히 1회인지 세면 실행 전에 갈린다
- 참조: [[logic-foundry-game]]

## 2026-08-09 — 상태기계 리셋 게이트가 '결국 이긴다'만 보고 있었다 (돌연변이 MISS 1건)
- 시도: 리셋 우선순위를 확인하려고 리셋을 켠 채 전이 조건을 올리고 0.2초(12틱) 뒤 단계를 읽었다
- 실패 이유: 리셋이 레벨이라, **전이가 이기는 구현도** 다음 틱에 리셋이 도로 1단계로 끌어내려 통과한다. '전이가 리셋을 이긴다' 돌연변이가 SURVIVED 로 나와서야 드러났다 — 게이트가 잰 성질이 "리셋이 결국 이긴다"였고, 물어야 했던 것은 "그 한 틱에 누가 이기는가"였다
- 대안·다음엔: 상태를 3단계로 올려 두고 리셋 ON 과 전이 상승을 **같은 틱에** 만든 뒤 tickOnce 한 번으로 자른다(전이가 이기면 4단계). 우선순위·경합 게이트는 여러 틱을 돌리지 말고 경합이 일어나는 그 틱만 볼 것
- 재사용 자산: 없음
- 참조: tasks/lessons/13-a-gate-can-measure-the-wrong-property.md

## 2026-08-09 — 씨앗 심기가 자기 목적을 배반했다 (평활 필터, 배선 전 y=0 고착)
- 시도: 평활 필터의 첫 평가에서 `n.state.y = xs` 로 입력값을 씨앗으로 심어, 이미 돌던 신호에 필터를 물릴 때 생기는 0→현재값 과도를 막으려 했다
- 실패 이유: 사람이 노드를 만드는 유일한 경로는 팔레트 클릭이고, 그 순간 노드는 **배선 없이** 생긴다. 편집기를 열어도 시뮬은 계속 돌고 고립 노드도 평가되므로, 16.7ms 안에 readIn=0 인 채로 첫 평가가 돌아 y=0 이 박힌다. 그 뒤 배선하면 y 가 이미 유한수라 재씨앗이 안 되고, 막으려던 과도가 정확히 그 자리에서 생긴다. τ=10·재고 60·임계 50 이면 거짓 경보가 17.9초 켜진다. 게이트 4종이 전부 배선을 첫 틱 **전에** 끝내고 입력을 0 으로 뒀기 때문에 "입력값 씨앗"과 "0 씨앗"이 구별되지 않았다 — 통과 케이스만 있는 게이트였다
- 대안·다음엔: 같은 파일의 출력 노드들이 이미 쓰는 규약(`if (!inputFed(g, n, 0)) break;`)을 상태를 가진 연산 노드에도 적용한다 — 안 물린 동안은 씨앗을 심지 말고 state 를 지운다. **상태를 초기화하는 코드를 쓸 때는 "그 초기화가 도는 시점에 입력이 이미 있는가"를 먼저 묻는다**
- 재사용 자산: 게이트 smooth.seedsFromRealInputWhenWired(배선 전/후를 갈라 재는 형태) + 음성 대조군 smooth.wiredStepStillFilters
- 참조: tasks/lessons/15-seeding-before-the-wire-betrays-its-own-purpose.md

## 2026-08-09 — 저장 게이트가 두 결함의 상쇄로 GREEN 을 냈다 (상태기계)
- 시도: 상태기계 저장/복원을 "2단계에서 저장 → 복원 후에도 2단계" 로 검사했다
- 실패 이유: 저장 시점에 네 전이 조건이 전부 참으로 붙들려 있었다. 노드 상태를 통째로 잃은 구현은 복원 시 1단계로 초기화되고, 붙들린 조건이 상승엣지로 읽혀 즉시 한 칸 튀어 **정확히 2단계**에 도착한다. 게이트는 2 == 2 를 보고 통과한다. 상태를 잃는 것과 헛전이하는 것, 결함 둘이 서로를 가렸다
- 대안·다음엔: 저장/복원 검사는 **초기화 기본값에서 한 칸 떨어진 상태를 고르지 않는다.** 3단계에서, 그리고 전이 조건을 내린 채 저장한다. 그리고 "단계가 남는가"와 "엣지 기억이 남는가"를 한 단언에 뭉치지 말고 쪼갠다 — 조건을 든 채 저장하는 별도 검사가 pe 손실을 잡는다
- 재사용 자산: fsm.survivesSave(조건 내림·3단계) + fsm.saveKeepsEdgeMemory(조건 유지·4단계) 두 갈래 형태
- 참조: tasks/lessons/15-seeding-before-the-wire-betrays-its-own-purpose.md

## 2026-08-09 — 표에만 있고 아무도 못 쓰는 기능이 둘 (문장 편집기)
- 시도: 새 노드를 문장 어휘에 넣으려고 37_rules.js 를 읽다가, 기존 기능 둘이 실제로는 닿을 수 없는 상태임을 발견
- 실패 이유: (1) 행동표의 `value: true` 플래그를 compileOneRule 이 한 번도 안 읽어서, '숫자를 화면에 띄운다' 가 조건의 참/거짓(1)을 띄웠다 — "숫자를 띄워 보기" 카드가 정확히 그 상태. (2) `when.math`(계산 한 단)는 컴파일러도 되읽기도 지원하는데 **57_ruleui.js 에 고를 자리가 없었다** — 카드로 만든 규칙 말고는 아무도 못 썼다. 덤으로 setPath 가 중간 경로(math 기본값 null)를 만나면 조용히 return 해서, UI 를 붙여도 설정이 사라질 뻔했다
- 대안·다음엔: 어휘표/정의표에 **플래그를 추가할 때는 그것을 읽는 쪽과 화면에 드러내는 쪽까지 같은 커밋에서 잇는다.** 그리고 새 필드를 넣으면 "플레이어가 이걸 고를 수 있나"를 클릭 경로 게이트로 잰다 — 저장/불러오기에서 이미 배운 '진입점이 없는 기능은 없는 기능'과 같은 자리다
- 재사용 자산: 게이트 rule.displayShowsTheNumber(+거짓일 때 0 음성대조군) · ui.mathDropdownExistsAndCompiles(+되돌리기)
- 참조: [[logic-foundry-game]]

## 2026-08-09 — 방어 코드를 특수 처리로 우회해 두고 그 코드에 돌연변이를 걸었다 (MISS 1건)
- 시도: setPath 가 중간 경로를 만들도록 고친 뒤, 그 분기를 겨냥한 돌연변이를 추가했다
- 실패 이유: 정작 UI 핸들러에서 `when.math.op` 를 특수 처리해 when.math 를 손으로 만들고 있었다. 그래서 고친 분기를 아무도 안 지나가고, 돌연변이가 SURVIVED 했다 — 검정할 수 없는 방어 코드는 등가 변형이다
- 대안·다음엔: 방어 코드를 넣었으면 **그 경로를 실제로 쓰게** 만든다. 특수 처리는 '(그대로) 선택 시 통째로 제거' 한 가지로 줄이고 나머지는 setPath 에 맡겼더니 그 돌연변이가 CAUGHT 로 뒤집혔다. 남길지 뺄지의 판정 기준은 "돌연변이가 잡히는가" 하나면 된다
- 재사용 자산: 없음
- 참조: tasks/lessons/15-seeding-before-the-wire-betrays-its-own-purpose.md

## 2026-08-09 — 물 지형을 넣었다가 되돌렸다 (시험 리그의 고정 좌표와 양립 불가)
- 시도: 유체 계통을 Factorio 그대로 만들려고 `world.terr` 에 호수를 생성하고, 물 위에는 못 짓게 하고, 취수 펌프만 물 위에 서게 했다. 렌더·배치 검사까지 다 붙였다
- 실패 이유: 시험 리그들이 **고정 좌표**에 짓는데 호수가 그 자리를 덮으면 배치가 null 을 돌려주고 드라이버가 TypeError 로 통째로 죽는다(power.rigBuilt FAIL + 전체 중단). 물을 끄면 이번엔 펌프가 설 자리가 없어 visual.everyBuildingDraws 가 정직하게 FAIL. 즉 물 지형과 고정 좌표 리그는 양립하지 않는다. **측정으로 확정**: 리그 좌표 143곳의 스폰 거리 중앙값 43, 최대 85 — "호수는 정착지 밖에" 규칙을 쓰려면 반경 85 이상을 비워야 하고 그건 맵(중심→모서리 113)의 대부분이다
- 대안·다음엔: 지형 대신 규칙을 단순화했다 — 펌프를 '지하수 펌프'로 바꿔 어디에나 서게 했다. 얻으려던 것(증기 버퍼로 깊어지는 제어 문제)은 지형과 무관하므로 손실이 없다. **새 지형·새 배치 제약을 넣기 전에 리그 좌표 분포를 먼저 재라** — 한 줄 스크립트로 5분이면 나오고, 안 재면 여기처럼 다 만든 뒤에 되돌린다
- 재사용 자산: 리그 좌표 분포 측정 한 줄 — tests/*.js 에서 `G.place|build('type', x, y` 를 정규식으로 뽑아 스폰(80,80) 거리 히스토그램
- 참조: [[logic-foundry-game]]

## 2026-08-09 — G.ent() 는 사본이다: 시험이 쓴 값이 아무 데도 안 갔고 게이트는 거짓 GREEN 이었다
- 시도: 증기 버퍼가 연료 단절을 견디는지 보려고 `G.ent(보일러).fuel = 0` 으로 연료를 끊었다
- 실패 이유: `G.ent()` 는 엔티티의 **스냅샷 객체**를 돌려준다(60_game.js). 대입은 그 사본에만 남았고 보일러는 계속 태우고 있었다. 게이트는 PASS 했지만 '연료가 끊긴 적이 없는' 통과였다. 짝지어 둔 음성 대조군(`증기 0 → 공급 0`)이 FAIL 로 나와서야 들켰다 — 대조군이 없었으면 거짓 GREEN 이 그대로 남았다
- 대안·다음엔: 시험은 **세계를 실제로 바꾸는 경로**로만 상태를 만든다. 여기서는 시험용 setter 를 새로 만들지 않고 `G.setInv('coal', 1)` 로 재고를 깎은 뒤 `putFromStock` 으로 정확히 4000 kJ 만 넣고 **실제로 다 타기를 기다렸다**. putFromStock 은 받을 수 있는 만큼 다 넣는다는 것도 그때 알았다(석탄 20개)
- 재사용 자산: 게이트 fluid.steamBuffersFuelOutage — 연료 잔량 0 을 함께 단언해 '끊긴 적 없음'을 원천 차단
- 참조: tasks/lessons/13-a-gate-can-measure-the-wrong-property.md

## 2026-08-10 — 청사진 게이트 2건이 기본값과 빈 리그에 기대어 통과하고 있었다 (돌연변이 MISS 2건)
- 시도: 청사진이 (1) 배선을 사본으로 갈아 끼우는가, (2) 걸친 건물을 빼는가를 게이트로 확인했다
- 실패 이유: (1) "사본 조립기가 **켜져 있다**"로 쟀는데 기계의 기본값이 켜짐이라, 참조가 통째로 끊긴 사본도 그대로 통과했다 — 켜짐은 지배의 증거가 아니다. (2) 리그에 **경계를 걸치는 건물이 하나도 없었다.** 영역 밖 연구소만 두고 "안 담긴다"를 봤는데, 스캔이 영역 안 타일만 훑으니 그건 당연한 것이라 아무것도 검사하지 않았다. 둘 다 돌연변이가 SURVIVED 로 드러냈다
- 대안·다음엔: (1) **기본값과 반대 방향으로 만든다** — 사본을 *끄게* 해서, 참조가 끊기면 도달할 수 없는 상태를 목표로 잡고 `logicForced` 까지 함께 단언한다. (2) 규칙을 재려면 그 규칙이 **갈라내는 대상을 리그에 넣는다** — 경계에 걸치는 조립기를 일부러 하나 놓았다. 게이트를 쓸 때 "이 성질을 만족하면서 틀릴 수 있는 구현이 있는가" 와 함께 "**내 리그가 그 구현을 만들어 낼 수는 있는가**" 를 묻는다
- 재사용 자산: 게이트 bp.wiringFollowsAndRetargets(끄기+logicForced 형태) · bp.capturesWholeBuildingsOnly(걸친 건물 포함 리그)
- 참조: tasks/lessons/15-seeding-before-the-wire-betrays-its-own-purpose.md

## 2026-08-10 — 오라클을 게임 자신에게서 읽었다: 속도를 8→12 로 바꿔도 게이트가 통과했다
- 시도: 열차 속도 게이트를 `Math.abs(travelSec - 20 / SP.trainSpeed) < 0.12` 로 썼다. SP 는 `G.spec()` 이 돌려주는 게임의 상수다
- 실패 이유: **기대값을 게임의 상수로 만들면 양변이 같이 움직인다.** trainSpeed 를 12 로 바꾸면 실제 주행도 빨라지고 기대값도 같이 빨라져서 차이가 0 으로 남는다 — 대조가 아니라 항등식을 확인한 셈이다. 돌연변이 '열차가 규격보다 빠르다' 가 SURVIVED 로 드러냈다. (다른 게이트들이 SP 를 쓰는 것은 대체로 무해하다 — 벨트처럼 SPEC 값에서 **파생된** 양을 대조하면 상수 하나를 바꿔도 양변이 같이 안 움직인다. 여기는 거리÷시간 = 속도라 정확히 상쇄됐다)
- 대안·다음엔: **오라클은 게임 밖에 둔다.** 설계값 8·5 를 시험 파일에 숫자로 박고, "게임의 수치를 바꾸려면 이 줄도 같이 고쳐라" 를 규약으로 삼는다. 게이트를 쓸 때 묻는다 — **이 기대값은 어디서 왔는가? 재는 대상과 같은 곳에서 왔다면 그건 오라클이 아니다.**
- 재사용 자산: 게이트 train.speedMatchesSpec (TRAIN_SPEED_ORACLE 상수를 시험 쪽에 둔 형태)
- 참조: tasks/lessons/16-the-oracle-must-live-outside-the-thing-it-measures.md

## 2026-08-10 — 관측 창이 0.125초뿐인 자리에서 재려 했다 (움직이는 열차 적재)
- 시도: '움직이는 열차에는 못 싣는다' 를 출발 타일에서 쟀다 — 출발 직후 화물이 안 느는지
- 실패 이유: 8타일/s 면 열차가 그 칸을 떠나기까지 **0.125초(7.5틱)** 뿐이고, 인서터 스윙은 1.2초다. '움직여도 싣는' 구현이 그 창을 대부분 비켜 가서 돌연변이가 살아남았다
- 대안·다음엔: 관측 창을 **구조적으로** 넓힌다 — 노선 한가운데에 인서터를 두고 여러 왕복(60초)을 돌렸다. 시뮬이 결정론적이므로 '실으려는 구현'은 반드시 한 번은 싣는다. 창이 좁은 성질은 리그를 바꿔서 넓히는 것이지, 운에 맡기는 것이 아니다
- 재사용 자산: 게이트 train.noLoadingWhileMoving (노선 중간 인서터 + 장시간 왕복 형태)
- 참조: tasks/lessons/16-the-oracle-must-live-outside-the-thing-it-measures.md

## 2026-08-10 — 이중 방어를 한쪽만 깨면 어느 쪽도 검정되지 않는다 (레일 검사)
- 시도: '열차는 레일 위에만' 을 UI(tryPlace)와 모델(addTrain) 양쪽에서 막고, UI 쪽 검사를 깨는 돌연변이를 걸었다
- 실패 이유: UI 검사를 지워도 addTrain 이 다시 막아서 열차가 안 생긴다 — 관측되는 차이가 없으니 등가 변형이다. 반대로 모델만 깨도 UI 가 먼저 막아 클릭 경로 게이트는 통과한다. **서로가 서로를 가린다.** 이 레포는 같은 형태를 넣기(canAccept 이중 방어)에서 이미 겪었다
- 대안·다음엔: **각 층을 그 층에서 잰다.** UI 를 거치지 않는 모델 게이트(`G.trainAdd(빈 땅) === null`)를 따로 세우고 모델 검사를 겨냥했다. 이중 방어 자체는 좋은 것이니 없애지 말고, 검정을 층마다 나눈다
- 재사용 자산: 게이트 train.addRejectsNonRail
- 참조: tasks/lessons/16-the-oracle-must-live-outside-the-thing-it-measures.md

## 2026-08-10 — 새 기능을 넣고 완주 드라이버를 안 늘려, 커버리지 게이트의 기준만 저절로 올라갔다
- 시도: 유체(건물 4종)와 기차(건물 3종·노드 3종)를 추가하고 모델·클릭·전수·돌연변이 게이트를 전부 GREEN 으로 맞춤
- 실패 이유: clear.js 의 allBuildingsUsed·allNodeKindsWired 는 기준을 게임의 건물·노드 목록에서 읽는다. 기능을 늘리면 기준이 자동으로 올라가는데 드라이버가 안 따라오면 그 기능은 40분 주행에서 한 번도 안 밟힌다. 결과: 11건 중 4건 RED, 안 쓴 건물 7종(pipe,pump,boiler,engine,rail,station,train)·안 쓴 노드 3종(fluid,station,traingo). 로직 노드 확장 때는 지켰던 규율을 유체·기차 때 두 번 연속 놓쳤다
- 대안·다음엔: 기능 추가의 완료 정의에 '완주 드라이버 확장'을 포함시킨다. 새 건물/노드는 clear.js 에서 실제로 세우고 배선해야 완료
- 재사용 자산: 없음
- 참조: [[verify-file-before-pronouncing]]

## 2026-08-10 — 완주 드라이버가 큰 것을 못 사는 이유는 흐름이 아니라 재고였다
- 시도: 증기 발전소·철도를 완주 드라이버에 추가하고, 재료가 모이면 사도록 afford 검사로 게이트
- 실패 이유: 이 공장은 철판이 초당 여러 장 흐르는데 재고는 언제나 0 이다 — 사이클마다 craft 가 쓰고 feed 가 기계 버퍼로 밀어 넣는다. 40분 내내 '철판 0/40'. 흐름이 넉넉한 것과 한 순간에 40장을 쥐는 것은 다른 문제다
- 대안·다음엔: 큰 구매는 (1) 사이클의 앞에 세우고(순서가 곧 우선순위) (2) 부품별로 사되 자리를 못 박고 (3) 기계가 안 받는 품목(파이프)으로 저축한다. 자리를 바꾸면 부분 건설이 재료 누수가 된다 — 실제로 펌프·보일러를 40분 동안 반복 건설해 철판이 0 이 되고 방어까지 무너졌다
- 재사용 자산: tests/clear.js 의 runBuyStages/steamParts/railTiles 패턴

## 2026-08-10 — 목표량을 올릴 때 같이 안 올라간 상수가 조용히 상한이 됐다 (CAP.steel=8)
- 시도: 철도용 강철 20개를 모으려고 STEEL_WANT 를 4→24 로 올림
- 실패 이유: feed 의 재고 상한표 CAP.steel 이 8 로 박혀 있었다. 강철 재고가 9 가 된 순간 feed 가 제련로를 영원히 건너뛰어 40분에 11개에서 멈췄다. 상한표는 분배기(강철 2)만 있던 시절 값이고, 목표를 올려도 따라 오르지 않았다. 증상은 '강철이 안 모인다'인데 원인은 '제련로가 재료를 안 받는다'라 재고만 봐서는 안 보인다
- 대안·다음엔: 목표량과 상한은 같은 식에 묶는다(steel: STEEL_WANT + 4). 진단은 재고가 아니라 '누계 생산 + 설비 상태'로 남긴다 — 그 두 줄이 원인을 즉시 갈랐다
- 재사용 자산: 없음

## 2026-08-10 — 보일러를 발전기와 같은 순위로 먹였더니 전력이 무너졌다
- 시도: feed 에서 generator 와 boiler 를 같은 분기로 처리(putFromStock 무제한)
- 실패 이유: 보일러는 1.8MW 를 태워 900kW 를 낸다 — 발전기보다 석탄 효율이 나쁘고 그 대가로 버퍼를 얻는 설비다. 같은 순위로 먹이면 발전기가 굶는다. 최저 전력 만족도 90.7% → 47.2% 로 떨어졌고, 이것은 유체 기능의 손해가 아니라 먹이는 순서의 문제였다
- 대안·다음엔: 발전기 먼저, 석탄 재고가 40 을 넘을 때만 보일러에 조금씩(4개). 채광 상한도 함께 올린다. 적용 후 94.3% 로 대조군보다 좋아졌다
- 재사용 자산: 없음

## 2026-08-10 — 완주 주행이 결정론적이라는 문서의 주장이 틀렸다 — 같은 코드로 4번 돌려 4번 다 달랐다
- 시도: 코드 변경 없이 clear.js 를 연속 실행하고 결과 비교
- 실패 이유: 열차 이동 65/90.5/95.5/97.4 타일, 습격 손실 3/7/8/10, 최저 전력 46.1~94.3%. powerHeld 게이트가 주행마다 PASS/FAIL 로 뒤집힌다 — 뒤집히는 게이트는 게이트가 아니다. src 에는 Math.random 도 시계 참조도 없고 드라이버가 G.pause(true) 로 루프를 세운 뒤 G.run(고정 dt) 로 직접 미는데도 갈린다. 내가 넣은 궤적 지문으로는 t=1500 까지 (엔티티·연구 기준) 동일했다 — 갈리는 지점은 후반이다. **원인 미확정**. README 는 이 흔들림을 '작은 변경에도 궤적이 흔들린다'로 적어 코드 변경 탓으로 돌리고 있었는데, 변경이 없어도 흔들린다
- 대안·다음엔: 게임에 이미 있는 G.stateHash()(엔티티별 재고·진행도까지 포함)로 지문을 다시 재서 갈리는 시각을 좁힌다. 그때까지 clear.js 의 defenseHeld·powerHeld 수치를 버전 비교에 쓰면 안 된다
- 재사용 자산: 없음

## 2026-08-10 — 완주 주행 비결정성 — 프레임 가설 반증, 변경 전 버전도 동일
- 시도: (1) 프레임이 원인이라 보고 PUMP_CYCLES=1e9 로 한 번에 밀어 프레임을 아예 안 끼움 (2) git show HEAD:tests/clear.js 로 변경 전 드라이버를 두 번 실행
- 실패 이유: (1) 프레임 없이도 두 주행이 갈렸다(습격 35/175/10 vs 29/145/7) → 렌더·UI 경로가 시뮬을 건드린다는 가설은 반증. (2) 변경 전 드라이버도 전력 47.9% vs 90.7% 로 게이트가 PASS/FAIL 을 오갔다 → 내가 넣은 것이 아니라 원래 있던 결함. README 가 '버전 간 비교용이 아니다'라며 적어 둔 두 숫자가 실은 같은 코드의 두 주행이었다
- 대안·다음엔: G.stateHash() 로 t=60/300/900/…/1500 지문을 찍으면 갈리는 시각이 주행마다 다르다(어떤 쌍은 t=900 에서, 어떤 쌍은 1500 에서). 다음 단계는 지문 간격을 더 좁혀 첫 불일치 틱을 찾고, 그 틱에서 무엇이 달랐는지 엔티티 단위로 대조하는 것. src 에는 Math.random 도 시계 참조도 없다
- 재사용 자산: tests/clear.js 의 FP_T/fpMark 궤적 지문 (G.stateHash 기반)

## 2026-08-10 — 결정론 비재현의 원인은 리셋이 안 되돌린 오염 확산 타이머였다
- 시도: 궤적 지문(G.stateHash)을 사이클마다 찍어 첫 불일치를 좁히고, 그 구간의 상태를 통째로 떠서 필드 단위로 대조
- 실패 이유: newGame() 이 gameTime·accumulator·진화도·신호버스·열차까지 되돌리면서 10_world.js 의 pollTimer 만 안 되돌렸다. 페이지가 열리고 드라이버가 reset 을 부르기까지 게임이 실시간으로 잠깐 돌아 pollTimer 에 [0,0.25) 잔여 위상이 남고, 그 위상이 확산·감쇠 스텝을 매 주행 다른 게임 시각에 떨어뜨렸다. 오염 0.1% 차이가 진화도→습격 규모로 증폭돼 40분 주행의 습격 손실과 최저 전력이 주행마다 달라졌다. **stateHash 가 오염 격자를 안 담아서** 지문은 끝까지 같은 값을 냈다 — 지문이 못 보는 상태는 지문이 없는 것과 같다
- 대안·다음엔: generateWorld 에서 pollTimer=0. stateHash 에 오염 격자 총량과 진화도 추가. 새 게이트 determinism.js 의 det.resetClearsPriorRun(리셋 전에 7.3초를 돌려도 같은 결과)이 이 결함을 잡는다 — 되돌린 빌드에서 FAIL 확인. 돌연변이 131번으로 등록
- 재사용 자산: tests/determinism.js (리셋·재현성 게이트, 음성 대조군 포함)

## 2026-08-10 — 계측이 갈린 것을 시뮬이 갈린 것으로 읽을 뻔했다
- 시도: 궤적 지문을 pump 배치가 끝날 때(afterCycles) 찍고 배치 크기 12 vs 4 를 비교
- 실패 이유: 지문을 배치 경계에서 찍으면 배치 크기가 **찍는 시각**을 바꾼다(12면 t=72, 4면 t=64 에 't=60 자리'를 채운다). 그 상태로 비교하니 t=60 부터 갈리는 것처럼 보였고, 하마터면 '배치 크기가 시뮬을 바꾼다'는 틀린 결론을 낼 뻔했다
- 대안·다음엔: 지문에 실제 시각을 같이 새기고(60@62.0:hash) 사이클마다 찍는다. 두 계측을 비교하기 전에 **같은 시각에 찍혔는지**부터 확인할 것
- 재사용 자산: 없음

## 2026-08-10 — 터렛 고리 기하를 두 번 손댔다가 둘 다 더 나빠졌다
- 시도: 남은 손실 1건이 바깥 고리에 혼자 선 터렛(turret@101,65)이라, (1) 안쪽 고리를 4→8자리로 늘리고 (2) 바깥 반경을 26→20 으로 당겨 봤다
- 실패 이유: (1) 초반 자재를 더 먹어 증기·철도·제어기가 밀렸다 — 습격 26→47회, 손실 3, 노드 커버리지 32→28. (2) 반경 20 은 터렛 자리가 공장 영역과 겹쳐 배치가 서로 밀어냈다 — 노드 26/32, 열차는 아예 안 움직였다. 이 판의 좌표는 서로 맞물려 있어 고리를 옮기면 공장 배치가 통째로 흔들린다
- 대안·다음엔: 기하는 그대로 두고 수량·시점(되먹임)만 조정한다. 손실 1 은 남기되 게이트 메시지에 뚫린 자리를 찍어(lostList) 다음 사람이 같은 탐색을 반복하지 않게 했다
- 재사용 자산: 없음

## 2026-08-10 — 방어를 작업 줄에 매달아 두면 피해가 난 뒤에 선다
- 시도: 터렛을 JOBS 큐에 jTurrets(0,4)/(4,9)/... 로 넣어 둔 상태 유지
- 실패 이유: 5~9번째 터렛이 줄 차례를 기다리다 t=1197 에야 섰고, t=1129 에 습격이 발전기 5대 중 3대를 부쉈다. 전력 만족도 48%로 떨어져 powerHeld 가 실패했는데, 원인은 석탄도 발전기 수도 아니라 **발전기가 부서진 것**이었다. 이 파일은 스스로 '방어는 계획이 아니라 되먹임이어야 한다'고 적어 놓고 줄에 두고 있었다
- 대안·다음엔: autoGen 옆에 autoTurret 을 두고 진화도에 비례해 세운다(기본 6기 + 진화 6%당 1기). 철판 재고가 늘 0 이라 20 짜리 구매가 성립 안 되므로 ironHold 로 32 를 잡아 둔다. 제어기 몫 12 를 남기지 않으면 노드 커버리지가 32→29 로 후퇴한다 — 방어를 고치며 커버리지를 깨면 고친 게 아니다
- 재사용 자산: 없음

## 2026-08-10 — 회전 게이트를 정사각형 리그로 세워 아무것도 검정하지 않았다
- 시도: 청사진 4x4 영역을 담아 회전 후 w/h 가 바뀌는지 검사
- 실패 이유: 정사각형에서는 가로세로가 바뀌어도 같은 수라 'rotateSwapsFootprint' 가 4x4 → 4x4 로 통과했다. 같은 판에서 rotateKeepsCost 는 bpInfo().cost 를 자기 자신과 비교하고 있었다(항상 참). 그리고 리그에 dir 1 짜리 분배기가 없어 '방향별 발자국' 규칙을 지우는 돌연변이가 MISS 로 살아남았다 — 규칙이 적용되는 입력이 리그에 없으면 그 규칙은 검정되지 않는다
- 대안·다음엔: 비대칭 영역(4x3)으로 담고, 비교값은 변경 전에 따로 찍어 두고, 검정하려는 규칙이 실제로 발동하는 입력을 리그에 반드시 포함시킨다
- 재사용 자산: 없음

## 2026-08-10 — '4회전 항등이 방향 회전도 잡는다'는 주장이 틀렸다
- 시도: 청사진 회전 게이트에서 rotateFourTimesIsIdentity 가 dir 회전 누락도 검출한다고 주석에 적고, 돌연변이 기대 게이트에 넣음
- 실패 이유: 방향을 아예 안 돌리면 네 번 돌려도 모든 항목의 dir 이 원본과 같다 — 항등이 그대로 성립한다. 돌연변이 시험이 MISS 로 그 주장을 반증했다. 좌표 회전은 4회전이 잡지만 방향은 못 잡는다
- 대안·다음엔: 게이트가 무엇을 잡는지 주장하기 전에 그 결함을 실제로 넣어 본다. 방향은 rotateTurnsDirections 가 따로 본다
- 재사용 자산: 없음

## 2026-08-10 — 40분 완주 드라이버는 저장 탱크를 살 여유가 없다 (9회 조정 실패)
- 시도: 강철 예산 24→26/28/30, 노선 6→4칸, 파이프 요구 제거(원가를 Factorio 공개값 철판20+강철5 로 정렬), 제어기 뒤로 순서 미루기, 탱크용 철판 재고 확보(ironHold 25)
- 실패 이유: 탱크는 철판 20 + 강철 5(=철판 25) 로 철 45장어치다. 이 공장은 철판 **재고**가 상시 0 이고(craft 와 feed 가 매 사이클 비운다) 강철 1개는 철판 5장이라, 강철 예산을 올릴 때마다 회로가 밀려 제어기 2·4 가 못 섰다(노드 커버리지 32→29). ironHold 는 craft 만 막고 runJobs 의 건물 비용은 못 막아 효과가 없었다. 최종 진단: '강철 24/5 철판 0/20' — 강철은 남는데 철판 재고가 한 순간도 20 에 못 닿는다
- 대안·다음엔: clear.allBuildingsUsed 를 21종 중 20종으로 RED 유지하고 게이트 메시지에 이유를 찍는다(탱크: 재료 대기 — 강철 x/5 철판 y/20). 게이트를 약화시켜 통과시키지 않는다. 드라이버의 물류 모델을 고치는 것이 진짜 해법이고 그건 별도 작업이다(tasks/lessons/12)
- 재사용 자산: clear.js 의 stageTank + tankWhy 진단

## 2026-08-10 — 완주 드라이버의 병목은 물류가 아니라 철 생산량이었다
- 시도: 생산 내역(prod.byRecipe)을 떠서 40분 산출을 품목별로 분해
- 실패 이유: 철판 5847 중 톱니 2612(45%)·탄약 1256(21%)·회로 460 이 나갔고, 녹색 연구팩의 병목은 벨트 128개였다. 벨트는 톱니를 거쳐 철에서 온다 — 연구가 7/8 에서 막히는 이유는 결국 철이다. 채광기+용광로 한 쌍은 철판 15장을 쓰고 20분에 370장을 돌려주는데, 작업 줄에 묶여 늦게 섰다
- 대안·다음엔: autoGen/autoCoal 옆에 autoIron 을 두고 초반(t<1200)에 되먹임으로 최대 6쌍 증설. 결과: 탱크를 살 수 있게 돼 건물 21/21·노드 32/32 로 커버리지가 닫혔고 완주 게이트가 10/13 → 11/13 이 됐다. 대가로 오염이 늘어 첫 습격이 세져 손실 1 → 3
- 재사용 자산: 없음

## 2026-08-10 — 제철 증설을 '방어가 선 뒤'로 미뤘더니 얻은 것을 전부 잃었다
- 시도: autoIron 에 TURRETS >= 4 게이트를 걸어 초반 오염 증가를 막으려 함
- 실패 이유: 회수 기간이 20분인데 증설이 t≈570 으로 밀리면서 회수 전에 주행이 끝났다 — 연구 7→6종, 탱크 못 삼, 노드 32→29 (게이트 11/13 → 9/13). 초반 손실 1건을 막으려다 세 가지를 잃은 셈
- 대안·다음엔: 철은 이르게 늘려야 값이 나온다. 초반 손실은 대가로 받아들이고, 방어는 따로(터렛 되먹임) 해결한다. 첫 습격 t≈450 인데 군수 연구가 t≈400 에 끝나 그 구멍은 구조적이다 — 메우려면 연구 순서나 유예 시간을 손대야 한다
- 재사용 자산: 없음

## 2026-08-10 — 계단으로 반올림된 신호를 미분해 놓고 미분이 틀렸다고 읽을 뻔했다
- 시도: 변화율 노드의 오라클로 타이머의 위상%(주기 동안 0→100 선형)를 씀 — 기울기가 정확히 100/주기 라고 봤다
- 실패 이유: 그 출력은 Math.round 로 정수화된 계단이라 대부분의 틱에서 변화가 0 이고 가끔 1 씩 뛴다. 날것의 변화율이 0.000 으로 나왔고, 평활을 걸었더니 주기 경계의 100→0 이 튀어 음수(-13.69)가 됐다. 노드는 멀쩡했고 입력이 미분 가능한 신호가 아니었다
- 대안·다음엔: 미분·기울기를 재는 게이트의 입력은 **연속량**이어야 한다. 취수 펌프가 붓는 물(1200/s, 반올림 없음)로 바꾸자 1200.0 이 나왔다. 게이트가 틀렸다고 말하기 전에 입력이 그 성질을 가질 수 있는지부터 본다
- 재사용 자산: 없음

## 2026-08-10 — 손으로 박아 둔 노드 이름이 두 번째 단항에서 갈림길이 됐다
- 시도: 문장 편집기의 계산 한 단(단항)에 변화율을 추가
- 실패 이유: 컴파일러가 graphAddNode(g,'smooth',...) 로 노드 종류를 손으로 박아 두고 있었다. 단항이 하나뿐이던 동안에는 맞았지만 둘이 되는 순간 '변화율을 골라도 평활이 걸린다' 가 된다 — 화면에는 고를 수 있는데 결과가 다른, 가장 알아채기 어려운 종류
- 대안·다음엔: 정의(RULE_MATHS)에 node·cfgKey 를 적고 컴파일러가 그것을 읽게 했다. 기존 돌연변이의 앵커(sm.cfg.tau=...)가 그 변경으로 무효(INVALID)가 돼 함께 다시 겨냥했다 — 코드를 고치면 그 줄을 겨냥하던 돌연변이도 같이 본다
- 재사용 자산: 없음

## 2026-08-10 — 구조적 구멍이라 단정한 것이 사실은 두 가지 순서 실수였다
- 시도: 첫 습격(t≈450)에 터렛이 1~2기뿐이라 '습격 유예 300초 vs 군수 연구 완료 t≈400 사이의 구조적 구멍' 이라고 README 에 적었다
- 실패 이유: 군수는 적팩 20개면 되고 시작 재고에 구리 30·톱니 30 이 이미 있다 — 즉 t≈150 에도 낼 수 있다. 드라이버가 늦었던 이유는 (1) 적팩 제작이 'cuLine(구리 용광로가 한 장이라도 구웠는가)' 를 기다리고 있었고 (2) 안쪽 터렛 고리를 위협 순이 아니라 동·남·서·북 고정 순서로 지어 3기 시점에 북쪽이 비어 있었다. 잃은 발전기(74,61)·전주(77,65)가 정확히 그 북쪽이다
- 대안·다음엔: 적팩은 전주 몫 구리 12 를 남기고 시작 재고로도 만들게 하고, 안쪽 고리도 바깥처럼 둥지 거리로 정렬한다. 결과: 손실 3 → 0, 완주 게이트 11 → 12건 통과(두 주행 동일). **'구조적'이라고 부르기 전에 그 구조가 정말 막혀 있는지 계산해 볼 것** — 여기서는 시작 재고만 세어 봐도 아니었다
- 재사용 자산: 없음

## 2026-08-10 — 연구 8/8 을 세 번 시도해 세 번 다 실패했다 — 병목은 적팩이고 톱니를 터렛과 다툰다
- 시도: (1) feed 의 배분 정렬을 부족분 오름차순 → 내림차순으로 뒤집기 (2) 톱니 재고 24개 확보 (3) 앞선 제철 증설(autoIron, 이미 반영됨)
- 실패 이유: 연구 총수요는 적팩 410 · 녹팩 280 인데 생산은 적팩 345 · 녹팩 395 다 — **녹팩은 남고 적팩이 65개 모자란다**. 새로 넣은 조립기 진단(asmDiag)이 원인을 정확히 보여 줬다: 적팩 조립기 셋이 'sci-red:.:in50' 즉 구리만 50 을 쥐고 톱니 0 으로 멈춰 있다. (1)은 공장 전체 배분이 흔들려 용광로 구역이 꽉 차고(배치 실패 2건) 열차가 멈췄다. (2)는 톱니가 터렛 재료(10개/기)이기도 해서 방어가 무너졌다(손실 0 → 24)
- 대안·다음엔: 적팩과 터렛이 같은 톱니를 놓고 다투고 이 공장에서는 터렛이 먼저다. 남은 길은 톱니 생산 자체를 늘리는 것(조립기 증설) 또는 탄약 소비를 줄이는 것 — 탄약이 철판의 21% 를 먹는다. asmDiag 를 상시 진단으로 남겨 다음 시도가 데이터에서 시작하게 했다
- 재사용 자산: tests/clear.js 의 asmDiag (조립기별 레시피·가동·입출력)

## 2026-08-10 — 재고를 보고 '여유'라고 읽었다가 방어가 통째로 무너졌다
- 시도: 탄약이 철판의 21~30%(699발=2796장)를 먹으니 후반 터렛 채움 상한을 110→70, 예비 탄창을 6+5T→6+3T 로 줄여 남는 철을 연구로 돌리려 했다. 근거로 든 것은 '격추 170 · 손실 0 · 터렛 12기' 라는 여유
- 실패 이유: 붕괴했다 — 손실 0 → 127, 최저 전력 100% → 0%, 연구 7 → 4종. 격추 170·손실 0 이라는 지표는 **탄약을 넉넉히 채워 둔 결과**였지 남는 몫이 아니었다. 결과 지표를 원인의 여유로 읽은 것
- 대안·다음엔: 방어 자원은 '충분해 보인다'로 줄이지 않는다. 줄이려면 먼저 소비율(탄약 소모/파도)을 재고, 상한을 낮춘 상태에서 파도를 견디는지 별도로 확인한다
- 재사용 자산: 없음

## 2026-08-10 — 톱니 조립기 증설이 두 번 다 실패했다 — 자재가 아니라 공간도 한계였다
- 시도: 적팩 조립기가 톱니를 못 받아 멈춰 있어(sci-red:.:in50) 톱니 조립기를 되먹임으로 증설. (1) t<1500 조건 (2) t>=900 으로 지연
- 실패 이유: (1) 시작 재고(회로 10·톱니 30·철판 60)로 **t=0 에 3대를 지어** 부트스트랩 자재가 사라졌다 — 공장이 아예 안 자람(건물 4/21 · 연구 0/8). 되먹임 장치의 조건에 '시작 재고로도 충족되는가' 를 안 봤다. (2) 시점을 미루니 이번엔 조립기 구역이 꽉 차 계획의 녹팩 조립기가 밀렸고(배치 실패) 연구는 그대로였다
- 대안·다음엔: 되먹임 증설 조건은 반드시 '시작 재고로는 성립 안 되는' 문턱으로 잡는다. 그리고 이 공장은 자재뿐 아니라 **공간**도 한계다 — 구역을 넓히지 않는 한 무엇을 넣으면 무엇이 밀린다
- 재사용 자산: 없음

## 2026-08-10 — 연구 병목이 '자재 부족'에서 '도착 시각'으로 옮겨 갔다
- 시도: 공장 반경 IN_R 22→25 확장 + 조립기 구역 확대 + 톱니 조립기 되먹임 증설(t>=600, 최대 3대)
- 실패 이유: 적팩 345→438(필요 410), 녹팩 331(필요 280)로 **총량은 요구치를 넘겼는데도 연구는 여전히 7/8** 이다. 마지막 automation-2 가 76% 에서 끝난다 — 재고는 상시 0 이고 랩 2대가 들어오는 즉시 소비하므로, 문제는 생산량이 아니라 그 생산이 **언제** 되느냐다. 증설 시점을 900→600 으로 당겨도 결과가 바이트 단위로 동일했다(그 조건에서는 애초에 안 걸렸다는 뜻)
- 대안·다음엔: 남은 24%를 메우려면 공장 전체 램프업을 10% 앞당겨야 한다 — 개별 손잡이(자재·자리·시점)로는 안 되고 초반 계획(JOBS 순서) 자체를 다시 짜야 한다. 총량 충족을 확인했으니 다음 사람은 '얼마나 만드나' 가 아니라 '언제 만드나' 에서 시작하면 된다
- 재사용 자산: 없음

## 2026-08-11 — 계획 순서를 당겼더니 하류가 통째로 굶었다 — 여섯 번째이자 마지막 시도
- 시도: 총량은 충족됐고 '도착 시각'이 문제라 판단해, 팩 사슬(벨트·인서터·녹팩 조립기)을 제련 확장 앞으로 당겼다. 제련은 autoIron 이 되먹임으로 늘리니 계획의 용광로 줄을 기다릴 이유가 없다고 봤다
- 실패 이유: 붕괴했다 — 제어기가 한 대도 못 서고(노드 0/33), 손실 145, 최저 전력 0%, 연구 7 → 6종. 앞당긴 조립기들이 회로와 톱니를 먼저 가져가 그 아래(제어기·터렛·발전기)가 통째로 굶었다. 이 계획은 한 줄을 당기면 아래가 무너지는 **팽팽한 균형**이다
- 대안·다음엔: 개별 손잡이 여섯 가지가 전부 실패했다(배분 정렬·톱니 재고·톱니 증설 초반/중반·탄약 절감·계획 순서). 남은 24%는 손잡이가 아니라 **계획 전체를 다시 설계**해야 닿는다 — 그 작업은 이 게이트 하나를 위해 40분 주행을 수십 번 다시 도는 일이므로, 착수 전에 값어치를 먼저 판단할 것
- 재사용 자산: 없음

## 2026-08-11 — 게이트 블록을 절 중간에 넣어 driver 주행이 통째로 중단 — 교훈 14를 적어 두고 또 밟았다
- 시도: 전주 공급반경·연결거리·벽 체력 게이트 3건을 driver.js 전력 절 '중간'에 삽입
- 실패 이유: 그 블록이 G.reset() 을 부르는데, 뒤에 오는 검사들은 리셋 전 세계를 쓰고 있었다. 결과가 '(중단 전)' 로만 찍히고 실검사 수가 줄었다 — 이 레포 tasks/lessons 14번에 이미 적혀 있던 함정
- 대안·다음엔: 리셋하는 블록은 반드시 그 절의 **끝**에 둔다. 새 게이트를 넣기 전에 그 블록이 G.reset/newGame 을 부르는지 먼저 본다
- 재사용 자산: 없음
- 참조: [[verify-file-before-pronouncing]]

## 2026-08-11 — 처리량 게이트의 오라클이 SPEC 파생값이라 SPEC 변경을 못 잡았다
- 시도: G.spec() 이 준 기대값과 시뮬 측정값을 대조
- 실패 이유: 양쪽이 같은 상수에서 나와 함께 움직인다. minerRate 0.5→0.25 실측에서 239건 전부 GREEN
- 대안·다음엔: 공개 숫자는 시험 파일의 문헌값 표(11.9 절 PUBLISHED)와 대조한다
- 재사용 자산: tests/driver.js 11.9 · src/60_game.js specRaw()

## 2026-08-11 — 같은 배수가 세 경로에 복제돼 있었다 — 저장 복원이 빠지면 '열면 느려지는 공장'이 된다
- 시도: 연구 효과 배수(벨트 2배, 기계 1.5배·전력 0.8배)를 연구 완료·저장 복원·시험 API 세 곳에 각각 적어 둠
- 실패 이유: 한 곳만 고치면 나머지가 조용히 갈라진다. 어느 게이트도 효과의 실제 크기를 재지 않아 세 경로가 일치하는지 확인할 방법도 없었다
- 대안·다음엔: 효과는 표 하나(TECH_EFFECTS)에 두고 applyTechEffects() 로 다시 계산한다. 게이트는 '연구했다'가 아니라 연구 전후 처리량·전력을 실제로 재서 비율로 본다
- 재사용 자산: src/25_entity.js applyTechEffects() · tests/driver.js 11.11 절

## 2026-08-11 — 연구 해금 목록이 실제 잠금과 20건 갈라져 있었다 — 강철 제련이 게임의 절반을 여는데 두 줄만 적혀 있었다
- 시도: TECHS[].unlock 은 손으로 적은 문자열, 실제 잠금은 건물·레시피·노드의 tech 표시
- 실패 이유: 두 곳을 잇는 검사가 없어 새 기능(유체·열차 계통)을 강철 뒤에 붙이면서 목록 갱신을 빠뜨렸다. 플레이어는 연구를 끝내고 나서야 무엇이 열렸는지 안다
- 대안·다음엔: 목록은 데이터에서 뽑아 대조한다(양방향: 잠긴 것이 목록에 있는가 · 목록의 것이 정말 잠겨 있는가). 사람이 두 곳에 같은 사실을 적는 구조를 보면 잇는 게이트부터
- 재사용 자산: tests/driver.js 11.12 절
