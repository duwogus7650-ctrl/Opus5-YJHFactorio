# LOGIC FOUNDRY — 프로그래머블 공정자동화

Factorio 계열 공정자동화 게임. 단일 HTML 한 장, 의존성 0, 오프라인 실행.
**차별점은 제어기(Controller)** — 공장의 판단 로직을 플레이어가 노드 그래프로 직접 배선한다.

```
dist/Logic-Foundry.html   ← 더블클릭하면 바로 돈다
```

---

## 왜 다른가

기존 공정자동화 게임에서 "공장이 언제 무엇을 할지"는 개발자가 정해둔 규칙이다.
Factorio의 회로망은 곁가지이고, 대부분의 플레이는 배치 최적화다.

여기서는 그 판단 자체가 플레이어의 설계 대상이다.

- 제어기는 매 틱 노드 그래프를 **한 번 위상순으로 평가**한다.
- 되먹임(순환) 배선은 **직전 틱의 값**을 읽는다 — 실제 순차논리의 레지스터 한 단과 같다.
  그래서 SR 래치·카운터·샘플홀드·PID를 그대로 배선할 수 있고, 조합논리와 순차논리가
  같은 화면에서 섞인다.
- 정답 회로는 없다. 같은 목표를 히스테리시스로도, 타이머로도, 부하차단으로도 푼다.

대표적인 과제 세 개:

| 문제 | 제어기가 없으면 | 제어기로 |
|---|---|---|
| 철을 탄약과 연구팩에 어떻게 나눠 쓰나 | 손으로 켜고 끈다 | 재고 히스테리시스(SR 래치)로 자동 전환 |
| 발전기를 안 늘리고 정전을 막으려면 | 못 막는다 | 전력 만족도 < 95%면 저우선 라인부터 차단 |
| 적이 올 때 방어에 전력을 몰아주려면 | 못 한다 | 적 근접 센서 → 생산 라인 차단 → 터렛 사격허가 |

---

## 튜토리얼

처음 켜면 좌하단에 9단계 튜토리얼이 붙는다. **"다음" 버튼이 없다** — 각 단계는
세계 상태를 보는 `check()` 하나이고, 공장이 실제로 그렇게 됐을 때만 넘어간다.
눌러서 넘기는 튜토리얼은 손이 배운 게 없기 때문이다.

```
1 채광기에 전기가 실제로 오는가        (net >= 0)
2 채광기 출구 앞에 벨트가 있는가
3 제련이 누적으로 일어났는가            (버퍼가 아니라 생산 카운터)
4 상자에 철판이 5개 모였는가
5 톱니가 만들어졌는가
6 연구가 실제로 진행됐는가              (researchProgress >= 1)
7 제어기를 놓고 편집기를 열었는가
8 제어기가 실제로 기계를 지배 중인가    (logicForced && fEnable)  ← 이 게임의 본체
9 터렛에 탄약이 들어갔는가
```

건너뛰기는 한 단계만 넘기고, 닫아도 진행은 유지된다(건설 판의 [튜토리얼] 버튼으로 다시 연다).
저장/불러오기에도 진행이 실린다.

## 시각 언어 — 계기반

UI 는 창이 아니라 **장비의 앞면**이다. 어두운 세계 위에 밝은 아노다이즈 알루미늄 판이
얹혀 있고, 라벨은 판에 각인돼 있고, **수치는 판에 박힌 어두운 계기창(well) 안에서만** 산다.
게임 화면과 UI 가 같은 톤이면 서로 섞여 읽히므로 일부러 반대로 갔다.

| 역할 | 값 |
|---|---|
| 금속판 | `#d8d4cc` (하이라이트 `#eae7e0` / 그림자 `#c2bdb4` / 테두리 `#6d675e`) |
| 각인 글자 | `#1c2024` · 보조 `#585e65` · 라벨 `#63696e` |
| 계기창 | 바탕 `#23282c` · 글자 `#eae6dd` (안쪽 그림자로 파인 느낌) |
| 상태색 | 운전 `#1f6b46` · 주의 `#96610a` · 정지·위험 `#b23a10` · 구조 `#2c5470` |
| 위험 줄무늬 | `#e2b21c` — 게임 속 벨트와 같은 노랑, **경보 한 곳에만** |

글꼴은 웹폰트를 쓰지 않는다(단일 HTML 오프라인). 로컬 폰트 조합으로 성격을 만든다:
라틴·라벨은 **Bahnschrift**(DIN 계열, 각인체 느낌 — `wdth 75` 로 압축), 한글 본문은
**Noto Sans KR**, 수치는 **Consolas**(등폭·표 정렬). 글리프 단위 폴백이라 한 요소 안에서
라틴은 각인체로, 한글은 본문체로 자동으로 갈린다.

모서리 라운드는 0(금속판은 각지다), 버튼은 눌리면 실제로 들어가고, 장식은 없다.
힘은 **밝은 판 × 어두운 계기창** 대비 한 곳에만 쓴다.

## 조작

| 키 | 동작 |
|---|---|
| `WASD` / 방향키 | 이동 · `휠` 확대 · `우클릭 드래그` 화면 끌기 |
| `좌클릭` | 배치 / 선택 · **드래그하면 벨트가 방향까지 자동으로 이어진다** |
| `우클릭` | 철거 (자원 전액 환급) |
| `R` | 회전 · `Q` 커서 아래 건물 복제 |
| `1`~`0` | 건설 단축키 · `T` 연구 · `P` 오염 표시 · `H` 도움말 |
| `F2` / `F3` | 저장 / 불러오기 (창고 패널 버튼과 같다, 저장 칸 1개) |
| 제어기 **좌클릭** | 노드 편집기 |

편집기 안: 출력 포트에서 입력 포트로 끌어 배선, 입력 포트 클릭으로 해제,
배경 드래그로 이동, 휠로 확대. **예제 불러오기**를 누르면 재고 히스테리시스 회로가
대상까지 물린 채로 들어온다.

---

## 수치는 전부 Factorio 공개값이다

임의로 정한 밸런스가 아니라 대조 가능한 오라클이다. `src/05_data.js` 상단에 출처가 적혀 있다.

| 항목 | 값 | 근거 |
|---|---|---|
| 벨트 | 1.875 타일/s → 레인당 7.5개/s, **합 15개/s** | yellow transport belt |
| 인서터 | 스윙 1.2s → **0.833개/s**, 13 kW | inserter |
| 채광기 | **0.5 광석/s**, 90 kW | electric mining drill |
| 제련 | **3.2s/판**, 180 kW | 시간=stone furnace, 전력=electric furnace |
| 조립기 | 제작속도 **0.75**, 155 kW | assembling machine 2 |
| 발전기 | **900 kW**, 석탄 4 MJ → 0.225 석탄/s | steam engine |
| 터렛 | 사거리 **18타일**, 5 dmg × 10발/s = 50 dps | gun turret + firearm magazine |
| 적 체력 | 소형 15 / 중형 75 / 대형 375 | biter |

이 표를 바꾸면 검증 하네스의 처리량 게이트가 즉시 어긋난다 — 일부러 그렇게 묶어 놨다.

---

## 빌드와 검증

```bash
python build.py                      # src/*.js → dist/Logic-Foundry.html 인라인
node tests/syntax_check.js           # 합쳐진 인라인 스크립트를 실제로 파싱
python tests/harness.py              # 모델 게이트 65건 (헤드리스 Edge)
python tests/harness.py uismoke.js   # 클릭 경로 게이트 31건 (합성 마우스/키 이벤트)
python tests/mutate.py               # 게이트가 실패를 잡는지 역검정 (소스를 고의로 깬다, 단독 실행)
python tests/crossbrowser.py         # 엔진 4개 × 드라이버 2개 = 8조합
python tests/balance.py              # 30분 시뮬 3시나리오 페이싱 측정 (게이트 아님)
python tests/shots.py                # shots/ 에 장면 스크린샷 4장 (Edge)
node tests/pwshot.js firefox shots/engine-firefox.png   # 엔진별 같은 장면 촬영
```

교차 브라우저는 Playwright 가 필요하다(한 번만):

```bash
npm i -D playwright && npx playwright install chromium firefox webkit
```

`harness.py <드라이버> <엔진>` 으로 엔진을 갈아끼운다. Chromium 계열은 `--dump-dom`,
나머지는 Playwright 러너(`tests/pwrun.js`)를 거치지만 **판정 로직은 harness.py 한 곳뿐이다** —
엔진마다 다른 채점기를 두면 어느 쪽이 맞는지 알 수 없게 된다.

### 검증 결과 (2026-08-08, 배포본 바이트 기준)

```
syntax_check.js       GREEN — 인라인 스크립트 파싱 통과
harness.py            GREEN — 실검사 65건 전부 통과 (고의 실패 1건 정상 검출)
harness.py uismoke.js GREEN — 실검사 31건 전부 통과 (고의 실패 1건 정상 검출)
mutate.py             GREEN — 돌연변이 24건 전부 해당 게이트가 검출 (놓침 0 · 무효 0)
crossbrowser.py       GREEN — 엔진 4개 × 드라이버 2개 = 8조합 전부 통과
balance.py            런타임 오류 0건 · 페이싱 표는 아래
```

| 엔진 | 모델 65건 | 클릭 31건 | 장면 렌더 |
|---|---|---|---|
| Edge (Chromium 151) | GREEN | GREEN | 기준 |
| Chromium 151 (Playwright) | GREEN | GREEN | 색 238종 · 엔티티 80 |
| **Firefox** (Playwright) | GREEN | GREEN | 색 249종 · 엔티티 80 · 페이지 오류 0 |
| **WebKit** = Safari 엔진 (Playwright) | GREEN | GREEN | 색 228종 · 엔티티 80 · 페이지 오류 0 |

네 엔진에서 같은 장면(`tests/scene-factory.js`)을 찍어 눈으로 대조했다 — `shots/engine-*.png`.
차이는 한글 폰트 메트릭뿐이고 배치·색·아이콘·전선·UI 는 동일하다. Safari 자체는 Windows 에서
돌지 않으므로 **WebKit 엔진으로 대신했다** (실기 Safari 는 미검증).

### 게이트가 무엇을 보증하는가

판정은 화면 문자열이 아니라 `window.__GAME` 이 돌려주는 JSON 필드로만 한다.

- **처리량** — 벨트 15개/s, 인서터 0.833개/s, 채광 0.5개/s, 제련 3.2s/판을 SPEC에서 계산한
  기대값과 대조. 벨트는 "실렸는가"뿐 아니라 "10초에 18.75칸까지 갔고 그 너머는 비었는가"로
  앞뒤를 함께 조인다.
- **물질수지** — 땅에서 뽑은 개수 == 세계에 존재하는 개수 증가분 (벨트 위 재고 포함).
  복제·소멸이 생기면 즉시 어긋난다.
- **에너지수지** — 태운 연료 kJ == 실제 공급 kW × 시간.
- **오염 배출** — 배출/s == 20 × 발전기 부하 × 0.02. 부하를 알기 위해 소비자는 오염을 내지
  않는 인서터만 쓴다.
- **전력** — 공급<수요일 때 만족도가 공급/수요와 같고, **개별 기계에 그 값이 실제로 내려가
  생산속도가 그 비율로 느려지는지**까지 본다(집계만 보면 "표시는 맞는데 안 느려지는" 고장을
  놓친다).
- **제어 로직** — 히스테리시스가 중간대를 **아래에서 올라올 때는 켜진 채, 위에서 내려올 때는
  꺼진 채**로 지나는지(여기가 래치와 단순 비교기가 갈리는 유일한 지점), 되먹임이 정확히 1틱
  지연인지, 제어 노드를 지우면 지배가 풀려 플레이어 의사로 돌아오는지.
- **전투/교착** — 터렛 사살 시간이 15hp ÷ 50dps 와 맞는지, 건물 벽 뒤에 가둔 적 6마리가
  45초 뒤 전부 굳지 않았는지.
- **결정론** — 같은 시드 2회 상태해시 동일, **다른 시드는 달라야 함**(음성 대조군이 없으면
  "항상 같은 값"인 고장도 통과한다).
- **시각** — 프레임버퍼를 직접 읽어 빈 화면을 검출하고, 그 검출기가 실제로 작동하는지
  아무것도 안 그린 화면으로 확인한다. 건물 13종이 전부 빈 땅과 다르게, 그리고 서로 다르게
  그려지는지도 본다.

### 페이싱 — 기계로 잴 수 있는 밸런스

`tests/balance.py` 는 30분치를 헤드리스로 돌려 표로 뽑는다. **게이트가 아니다**(합격선이 없다).
모델링 가정은 "기계가 굶지 않는 완벽히 배선된 공장"이라 여기 나오는 오염·진화 속도는
**실제 플레이의 상한**이다. 출력 맨 위에 가정을 같이 찍는다.

| 시나리오 (30분) | 첫 습격 | 첫 손실 | 30분 진화도 | 누적 습격 |
|---|---|---|---|---|
| A 무방비 (채광5·용광로6·조립3·발전2) | 6분 | 6분 | 23% | 2파 |
| B 방어 (같은 공장 + 터렛 6) | 6분 | **없음** | 37% | 22파 / 110마리 |
| C 대형 (2배 규모 + 터렛 4) | 6분 | 6분 | 35%(20분) | 30파 / 150마리 |

읽는 법: **무방비 공장은 6분에 부서지기 시작하고, 터렛 6대면 110마리를 손실 0으로 막는다.**
크게 지을수록 오염이 늘어 습격이 빨라진다(C가 A보다 파도가 15배 많다). 초반 5분은 유예다 —
없으면 잘 지은 것이 벌이 된다.

보조 측정: 발전기 1대가 조립기 5대를 감당(155kW 기준 이론상 5.8대) · 시작 철광맥 1개 +
채광기 1대는 **151분에 4509개** 채굴 후 고갈(오라클 0.5개/s × 151분 = 4530, 오차 0.5%).

### 게이트 자체의 검정

`mutate.py` 는 소스를 한 곳씩 고의로 깨뜨리고 지목한 게이트가 실제로 뒤집히는지 본다.
바이트로 읽고 쓰고 대조하며(개행 변환에 속지 않기 위해), 문법 오류를 낸 돌연변이는
`INVALID` 로 분리한다(모든 게이트가 무너진 것을 "잡았다"로 오독하지 않기 위해).

이 역검정은 여러 번 **놓침(MISS)** 을 냈고, 그때마다 게이트가 실제로 고쳐졌다:
전력 게이트가 집계만 보고 개별 기계를 안 봤고, 히스테리시스 검정이 한 방향뿐이었고,
시각 총조사가 그림자만으로 통과했고, 테스트 훅 하나가 결함을 가리고 있었다.

---

## 출고 전 적대적 리뷰에서 고친 것

게이트 57건이 전부 GREEN인 상태에서 5개 차원(시뮬·로직·저장·전투·렌더UI)으로 독립 리뷰를
돌리고, 발견마다 **반증을 시도하는** 검증을 붙였다. 38건 중 상위 9건을 검증해 전부 실기
재현으로 확정됐다. **게이트는 내가 생각해 본 실패만 막는다**는 것이 이 단계의 결론이다.

| 심각도 | 결함 | 왜 게이트가 못 봤나 |
|---|---|---|
| critical | 자원을 다 쓰고 저장하면 불러올 때 공장이 통째로 사라짐(벨트 60개→0) | `free` 가 비용 *차감*만 뜻하고 *검사*는 안 뜻했다. 저장 테스트가 재고 무한 상태에서만 돌았다 |
| critical | 남·서향 분배기의 셀과 점유 사각형이 어긋나 인계 불능·유령 엔티티 | 테스트 전체에 `splitter` 문자열이 0건. R 키는 4방향을 도는데 한 방향만 썼다 |
| critical | 불러오기 id 충돌로 엔티티 소실(20개→4개)·영구 사용불가 타일 | 철거를 4회 이상 한 저장본에서만 발생 |
| high | 정체된 벨트 앞 인서터가 못 놓는 아이템을 쥔 채 영구 교착 | peek(검사)과 take(실행)의 선택 규칙이 서로 달랐다. **기계 앞 벨트는 늘 정체돼 있다** |
| high | 망에서 분리된 발전기가 마지막 부하율로 영원히 연료를 태우고 오염 배출 | 전주 철거 경로가 테스트에 없었다 |
| high | 고갈된 광맥 위 채광기가 불러오기에서 사라짐 | 위 `free` 결함과 같은 뿌리 |
| high | 인스펙터를 0.2초마다 통째로 다시 그려 열어 둔 드롭다운이 닫힘 → 레시피 선택 불가 | 모델 게이트는 DOM을 안 본다 |
| high | 제어기로 발전기를 끌 수 없었고, 제어기가 전력을 요구해 자기 전원을 끊으면 발진 | 폐루프 검사가 없었다 |
| medium | 용광로가 '아무 제련 레시피'로 판정해 안 쓰는 재료가 입력 버퍼를 막음 | — |
| medium | 벌목한 나무가 계속 오염을 흡수 · 되먹임 레지스터가 저장 안 됨 · 적 없을 때 근접거리 0 | — |
| — | 저장/불러오기에 **UI 진입점이 없었다** (README가 기능이라 적어둔 것을 부를 방법이 없음) | 하네스는 API를 직접 불렀다 |

확정 결함마다 회귀 게이트를 새로 달았고(12건), 돌연변이 역검정에도 각각 추가했다.

---

## 구조

```
src/00_core.js      상수, 결정론 난수, 잡음, 오류 수집
src/05_data.js      아이템·레시피·건물 스펙·기술트리  ← 수치 오라클
src/10_world.js     격자, 지형/광맥 생성, 오염 격자
src/20_belt.js      2레인 벨트, 분배기, 위상정렬 처리 순서
src/25_entity.js    배치/철거, 기계, 인서터
src/30_power.js     전력망(유니온-파인드), 만족도, 전선 MST
src/35_logic.js     ★ 노드 그래프 런타임 (제어기)
src/40_enemy.js     둥지, 습격파, 전투, 터렛
src/45_render.js    Canvas2D 렌더러 (타일 좌표계)
src/50_ui.js        HUD, 건설, 입력, 인스펙터, 연구, 미니맵
src/55_logicui.js   노드 편집기 (DOM 노드 + SVG 배선)
src/60_game.js      루프, 저장/불러오기, __GAME 테스트 API
```

시뮬레이션은 **60 UPS 고정 스텝**이고 픽셀을 모른다. 렌더러만 픽셀을 안다.

틱 순서는 `로직 → 전력 → 기계 → 벨트 → 오염 → 적`. 로직이 먼저여야 이번 틱에 끈 기계가
이번 틱 전력수요에서 빠진다 — 반대로 두면 부하 차단이 한 틱씩 늦어 전력이 덜덜 떨린다.

---

## 알려진 한계

- 유체(석유·증기)와 기차가 없다. 발전은 석탄 직접 연소로 단순화했다.
- 지하 벨트가 없다. 대신 분배기와 벨트 게이트가 있다.
- 플레이어 아바타가 없다 — 당신은 제어계이지 사람이 아니다. 이동 제약이 없는 대신
  건설 범위 제한도 없다.
- **제어기는 전기를 쓰지 않는다.** 전력을 요구하면 부하 차단 배선이 자기 전원을 끊고
  자멸한다(Factorio의 회로망도 같은 이유로 무전력이다).
- 저장은 `localStorage` 한 칸(약 140 KB). 여러 슬롯은 없다. `F2`/`F3` 또는 창고 패널 버튼.
- 검증은 헤드리스 4엔진(Edge·Chromium·Firefox·WebKit) 기준이다. **실기 Safari 와 모바일,
  그리고 "재미"는 미검증** — 페이싱 표는 규칙이 맞는지만 보여주고 재미는 못 잰다.
- 터치 입력을 고려하지 않았다. 마우스+키보드 전제다.
- 페이싱 측정은 "기계가 굶지 않는 완벽한 공장" 가정 위의 **상한**이다. 실제 플레이는 더 느리다.
